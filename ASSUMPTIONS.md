# ASSUMPTIONS — 置いた仮定とその理由

資料に無いために明示的な仮定を置いた項目です。
アプリ内「根拠」タブで `ASSUMPTION` を絞り込むと、同じものを値つきで一覧できます（41 件）。

**原則**: 不明な内部寸法を、確認済みの事実であるかのように補完してはいません。
仮定はすべてこのファイルとデータ JSON の `note` に理由を書いています。

---

## A. ジャック内部の接点（最も重要）

メーカーのデータシートにもメーカー CAD にも、**内部接点ばねの寸法は含まれていません**。
外形図と回路記号しか公開されていません。したがって内部接点は独自に構成しています。

### A-1. 接点の軸方向位置 — ASSUMPTION（範囲は絞れるが、値は決まらない）

| 接点 | 前面基準の軸位置 | 導出の根拠 |
|---|---|---|
| Sleeve 接点 | 3.2 mm | 完全挿入 (d=14) で s=10.8 となり、Sleeve 導体 9.0〜14.0 の内側に収まる |
| Ring 接点ばね | 7.1 mm | 完全挿入で s=6.9。Ring 導体 5.5〜8.3 のほぼ中央（前後に約 1.06 mm の余裕） |
| Tip 接点ばね | 11.4 mm | 完全挿入で s=2.6。Tip 導体 0〜4.8 の中で半径が十分ある位置 |

これらは「完全挿入時に正しく結線されなければならない」という制約と、
基板レイアウト図の端子位置（FACT）から**範囲を絞った**うえで選んだ値です。
梁の長さが分からないので、範囲は狭められても**値そのものは決まりません**。

> 2026-07-31 まで、ここを DERIVED（導出）としていました。**幾何学的な導出ではないので
> ASSUMPTION へ下げました。** 同じ性質の 4極側（`trrs.jack.contact.*`）は最初から
> ASSUMPTION としており、格付けが割れていたのを揃えた形です。
> なお Sleeve 接点の 3.2 mm については、入口ブッシングとの関係で別の論点があります
> → [UNKNOWNS.md](UNKNOWNS.md) §3-8。

**成立範囲と、そこを振ったときの影響**（[docs/SENSITIVITY.md](docs/SENSITIVITY.md)）:

| 接点 | 採用値 | 成立範囲 | 振ると動くもの |
|---|---:|---|---|
| Sleeve | 3.2 mm | 0.45〜4.55 | **橋絡の深さ**。パッド幅と同時に振ると 6.96〜13.14 mm。ここが最大の不確かさ |
| Ring | 7.1 mm | 5.98〜8.23 | ブレーク接点が開く深さ 6.93〜9.18 mm。橋絡の深さは動かない |
| Tip | 11.4 mm | 9.48〜13.40 | 全接点が正常になる深さ 12.34〜13.76 mm。橋絡の深さは動かない |

**橋絡の深さを動かすのは帰線（Sleeve）接点だけ**で、Tip / Ring 接点をどこに置いても
11.76 mm から動きませんでした。不確かさの発生源が 1 つに絞れています。

### A-2. 接触パッドの幅 — ASSUMPTION

| 接点 | パッド幅 | 理由 |
|---|---|---|
| Tip / Ring | 0.55 mm | 絶縁帯 0.7 mm より**狭い**。だから絶縁帯の上に完全に乗って回路が開き得る |
| Sleeve | 0.90 mm | 帰線側は接触面積を広く取るのが一般的。この幅だと絶縁帯をまたぎ得る |

**この 2 つの数値に一次資料はありません。** 橋絡が起きる深さを直接決めるので、影響は小さくありません。

振ってみた結果（[docs/SENSITIVITY.md](docs/SENSITIVITY.md)）:

| パッド | 採用値 | 成立範囲 | 振ると動くもの |
|---|---:|---|---|
| Sleeve | 0.90 mm | 0.01〜5.00 | **橋絡の深さ**。軸位置と同時に振ると 6.96〜13.14 mm |
| Ring | 0.55 mm | 0.01〜2.80 | ブレーク接点が開く深さ 6.93〜8.29 mm。橋絡の深さは動かない |
| Tip | 0.55 mm | 0.01〜4.80 | **何も動かない**（橋絡・ブレーク・全正常のいずれも不変） |

#### 橋絡の成立条件は「パッド幅 vs 絶縁帯幅」ではない

パッド 0.9 mm が絶縁帯 0.7 mm より広ければ橋絡する ―― これは**軸方向しか見ていない誤り**です。
2026-07-31 の公開前監査で気づいて直しました。

プラグの導体には高さの差があります。**Tip の最大半径は 1.600 mm（φ3.20）**で、
**Ring / Sleeve の 1.750 mm（φ3.50）より 0.15 mm 低い**のです。

接点はパッド範囲内で一番高い面に乗って押し開かれます。したがって、

- **第1絶縁帯（Tip↔Ring）** … 接点は Ring（1.750）に押し上げられ、0.15 mm 低い Tip 面には届かない → **橋絡しない**
- **第2絶縁帯（Ring↔Sleeve）** … 両側とも 1.750 で同じ高さ → **橋絡する**

正しい成立条件は次のとおりです。

> **同じ高さの導体面どうしの間隔（0.700 mm）が、パッド幅（0.90 mm）より狭いこと**

0.700 mm は、半径 1.750 の平坦面が途切れてから次に始まるまでの距離です。
図面のベクタでは導体と絶縁帯の境界が 1 本の垂直線で描かれていて面取りが無いため、
この間隔は絶縁帯の記載幅 0.7 mm とそのまま一致します。

> 2026-07-31 まで、ここを **0.776 mm** と書いていました。図面を 1200 dpi でラスタ化して
> 測ったとき、線の太さ（0.302 pt ≒ 0.107 mm）を段の面取りと読み違えて、
> 境界の両側に 0.09 mm の面取りを置いていたためです。ベクタ座標に戻して確認したところ、
> そのような面取りは作図にも寸法記載にも存在しませんでした。
> **橋絡するという結論は変わりません**（0.700 も 0.776 もパッド幅 0.90 より狭い）が、
> 数字の出どころが誤っていたので訂正します。

#### 接触ドームの追従量 — ASSUMPTION

上の判定を実装するには、「接点が押し出された位置より、どれだけ低い面まで追従できるか」を
決める必要があります。**0.05 mm** としました（`model.contact.complianceMm`）。

一次資料はありません。上下の破綻点から逆算した値です。

| compliance | 起きること |
|---|---|
| 0.0008 mm 未満 | 完全剛体に近づき、完全挿入時に Tip 接点が導通しなくなる（下記） |
| **0.02〜0.10 mm** | **採用範囲**。第2絶縁帯だけで橋絡する |
| 0.1538 mm 以上 | 0.15 mm の段差をまたぐ誤った同時接触が復活する（公称寸法の場合） |

下限が生じるのは、Tip 後端が逆テーパで傾いている（傾き 0.0799）ためです。
傾いた面の上では導通帯の幅が `compliance ÷ 傾き` になり、これが導通の下限
0.01 mm を割ると接触が消えます。導体と絶縁帯の段差とは関係がなく、
面取りを削除しても下限は 0.0008 mm のまま変わりませんでした。

実物の接触ドームがどれだけ追従するかは未確認です（[UNKNOWNS.md](UNKNOWNS.md) §3-6）。

**ただし、この値の不確かさは「Tip↔Ring で橋絡しない」という結論を脅かしません。**
接点が Tip 面に届く条件は `1.600 ≥ 1.750 − compliance`、すなわち
`compliance ≥ 0.150 mm` で、**パッド幅にも接点位置にも依存しない**からです。
実測したしきい値は 0.153752 mm で、この 0.150 のすぐ上にあります。
採用値 0.05 はその約 1/3.1 です（[docs/SENSITIVITY.md](docs/SENSITIVITY.md) §2-1）。

**ただし床の 0.150 は FACT だけでは決まりません。** 段差は
`plug.bodyRadius`（FACT・公差 ±0.025）と `plug.insulatorRadius`（**DERIVED**・図面実測
φ3.20〜3.22）の差で、**両端とも幅を持ちます**。両方を悪い側へ振った最悪コーナー
（φ3.45 × φ3.22）ではしきい値が **0.118875** まで落ちます。

| | しきい値 | 採用値 0.05 との比 |
|---|---|---|
| 公称 | 0.153752 | 3.08 倍 |
| 最悪コーナー | 0.118875 | **2.38 倍** |

**採用値では最悪コーナーでも橋絡しません**が、compliance の採用範囲の上端 0.10 と比べると
余裕は 1.19 倍しかありません（同 §6-1、[UNKNOWNS.md](UNKNOWNS.md) §4-1b）。

### A-3. 自由半径・ばね定数・許容たわみ — DERIVED / ASSUMPTION

- Ring / Sleeve の自由半径 1.40 mm … プラグ半径 1.75 mm に対し公称たわみ 0.35 mm
- Tip の自由半径 1.10 mm … Tip 導体は接触位置で半径 1.406 mm しかないため、これより小さくしないと押付が得られない
- ばね定数 3.1 N/mm … 完全挿入時の押付が **0.8〜1.3 N/接点**（TE Connectivity 資料の値）に収まるよう設定
- 許容たわみ 0.85 / 0.95 mm … Tip 接点が φ3.5 面に乗ったときの 0.65 mm を許容する値

ばね定数は「公称押付力レンジに合わせた逆算」であり、実測でも FEA でもありません。

### A-4. ブレーク接点の開放しきい値 — ASSUMPTION

信号ばねのたわみが **0.05 mm** を超えたら開く、としています。
Same Sky の技術解説から「信号ばねが押し広げられてノーマル接点から離れる」構造であることは確認しましたが、
**しきい値の数値はどこにも書かれていません**。

この値は「ブレーク接点がいつ開くか」を直接決めます。小さくすれば早く開き、大きくすれば遅く開きます。

### A-5. 接点の円周方向位置

- Tip 接点 270°、Ring 接点 90° … 基板レイアウト図で端子 3 が軸の左（−5.05 mm）、端子 2 が右（+4.55 mm）にあることから **DERIVED**
- Sleeve 接点 0° … 端子 1 は軸上（横位置 0）なので図面から確定できず **ASSUMPTION**

### A-6. 接点ばねの 3D 形状

板厚 0.25 mm、板幅 1.6 mm、根元半径 2.75 mm、リードインの立ち上がり 0.85 mm などは表示専用の ASSUMPTION です。
たわみは「根元まわりの剛体回転」で近似しています（実際の片持ち梁は曲率分布を持つため、これは表示上の近似です）。
**接触判定はこの 3D 形状を一切参照していません。**

---

## B. プラグ形状

### B-1. Tip 導体と第1絶縁帯の境界を 4.8 mm とした — DERIVED

図面は金属と樹脂を塗り分けないため、この境界は直接読めません。2 つの根拠から 4.8 mm としました。

1. 図面実測で **s = 4.902 に分割線**が検出された
2. 3.5 mm プラグの絶縁帯幅は複数社の図面で **0.7±0.1** に揃っており、Ring 前端 5.5 から 0.7 を引くと 4.8 になる

φ3.20 への段は s = 4.292 にありますが、その 4.292〜4.8 は **Tip 金属の後端を絞った部分**と解釈しました。

> **この解釈を変えると絶縁帯の幅が変わります。** 境界を 4.292 とすると第1絶縁帯の幅が
> 1.208 mm になり、第2絶縁帯（図面記載 0.7）と揃わなくなります。
> 現在の解釈のほうが、記載寸法とも業界慣行とも整合すると判断しました。
>
> なお以前ここには「4.292 にすると Tip↔Ring の橋絡が起きなくなる」と書いていましたが、
> **その感度分析は無効になりました。** A-2 のとおり、Tip 導体は Ring より 0.15 mm 低いため、
> 境界を 4.8 のままにしても第1絶縁帯では橋絡しません。どちらの解釈でも結論は変わりません
> （＝この境界の取り方に対して、橋絡の判定は頑健です）。

### B-2. 電気的境界は「記載寸法の演算」を採用

| 境界 | 採用値 | 図面実測値 | 差 |
|---|---|---|---|
| Ring 前端 | 5.5（= 14 − 8.5） | 5.487 | 0.013 mm |
| Ring 後端 | 8.3（= 14 − 5 − 0.7） | 8.306 | 0.006 mm |
| Sleeve 前端 | 9.0（= 14 − 5） | 8.992 | 0.008 mm |

記載寸法はすべて FACT なので、その演算結果を正としました。
外形プロファイルの段の位置も同じ値を使います。図面では段が 1 本の垂直線で描かれており、
面取りは作図にも寸法記載にも存在しないためです（実物には何らかのエッジ処理があるはずですが、
その寸法は一次資料にありません → [UNKNOWNS.md](UNKNOWNS.md) §4-4）。

### B-3. ハンドル部

肩カラー長 1.016 mm、本体長 23.98 mm などは実測または記載寸法からの差し引きです。
ケーブル長 14 mm は表示専用の ASSUMPTION（部品仕様ではありません）。
**ハンドル部は接触判定に一切使っていません。**

---

## C. 接触品質スコア（仕様 §7）

不完全接触時の抵抗値を一次資料で校正できないため、**Ω 値を出していません**。
代わりに 0〜1 の相対スコアを次式で出しています。

```
品質 = (0.35 × 有効接触幅比 + 0.65 × 押付力比)
       × (1 − 汚れ係数) × (1 − 0.4 × 摩耗) × (1 − 断続ノイズ)
```

- 重み 0.35 / 0.65 … 接触抵抗が接触幅より押付力に強く依存することを反映した配分。**実測校正ではありません**
- CLOSED / TOUCH_UNSTABLE の境界 0.55 … ASSUMPTION
- 導通とみなす最小重なり幅 0.01 mm … ASSUMPTION

完全接触時に限り、メーカー公称の接触抵抗上限 ≤30 mΩ を「仕様値」として別枠で表示しています。
**シミュレーション値とメーカー保証値は画面上でも分けています。**

---

## D. 挿抜力（仕様 §8）

**有限要素解析は行っていません。実物と同一のばね力を再現したとは主張しません。**

手法はエネルギー法です。接点 1 本あたり `F = N·(μ + dδ/dd)`。
`dδ/dd` はプラグ外形プロファイルの傾きを中心差分で求めた値なので、
**曲線の形（入口摩擦・テーパ乗り越えの局所ピーク・完全挿入位置の保持・抜去時のヒステリシス）は
幾何から出てきます**。仮定しているのは係数だけです。

| パラメータ | 値 | 区分 |
|---|---|---|
| 摩擦係数（挿入 / 抜去） | 0.35 / 0.30 | ASSUMPTION |
| 入口ブッシング摩擦 | 0.09 N/mm（上限 1.2 N） | ASSUMPTION |
| 完全挿入位置のデテント | 1.4 N（σ 0.6 mm） | ASSUMPTION。プラグ外形に保持溝が無いための明示項 |
| 全体校正係数 | 1.45 | ASSUMPTION |
| ランプ項 `dδ/dd` の上限 | 0.48 | ASSUMPTION。下記 |

**ランプ項の上限（`force.maxRampSlope`）について。**
導体と絶縁帯の境界は幅ゼロの垂直な段なので、そこではたわみが 0.15 mm 不連続に跳びます。
`dδ/dd` は原理的に発散し、中心差分の刻み（0.005 mm）がそのまま値を決めてしまいます。
上限を置かないと、走査刻みを変えるだけでピーク挿入力が **17.8 N**（0.02 mm 刻み）にも
**28.2 N**（0.005 mm 刻み）にも変わり、物理量になりません。

実物の接触ドームは点ではなく有限の曲率を持つので、段の角に当たってから登り切るまでに
水平距離 `L = √(2RΔ − Δ²)` を要します。ドーム半径 `R = 0.4 mm`、段の高さ `Δ = 0.15 mm`
と仮定すると `L = 0.312 mm`、平均の傾きは `0.15 / 0.312 = 0.48`。
**`R` に一次資料はありません**（→ [UNKNOWNS.md](UNKNOWNS.md) §3-7）。

この上限を入れると、走査刻み 0.02 mm と 0.005 mm でピークが一致し、
ピークは段の通過ではなく完全挿入位置のデテントで決まるようになります。

結果: 挿入ピーク **5.27 N**、抜去ピーク **5.28 N**。
Lumberg 1503 09 の公称 **3–20 N**（gauge plug 測定）の範囲内であることを確認しました。
**校正係数はこのレンジに合わせるために置いたものです**（レンジに合ったことは検証ではなく設計です）。

### この 5.27 N を単一値として読まないでください

上の 9 件はすべて ASSUMPTION です。成立範囲いっぱいに振って測りました
（[docs/SENSITIVITY.md](docs/SENSITIVITY.md) §3-4）。

| | 値 |
|---|---|
| `calibrationScale` は**純粋な倍率** | 1.0 → 3.64 N ／ **1.45 → 5.27 N** ／ 2.0 → 7.27 N |
| 校正係数を固定し、物理 8 件を同時に振る（2 水準・256 通り） | **1.5 〜 11.2 N** |
| 公称 3–20 N を外れた構成 | 32 / 256（いずれも下側） |
| ピークが完全挿入位置（デテント支配）だった構成 | 112 / 256 |

**5.27 N は幅 7.3 倍の中の 1 点**で、その中心を決めているのは独立した根拠を持たない校正係数です。

意味があるのは**曲線の形**（入口摩擦の立ち上がり・段の乗り越え・完全挿入位置の保持・
抜去時のヒステリシス）のほうで、こちらは幾何から出ており係数を振っても変わりません。

なお `differentiationStep` と `maxRampSlope` を範囲いっぱいに振ってもピークは 5.27 N のまま
動きません。**上限を置いた目的（走査刻みに答えを支配させない）は達成されています。**

---

## E. 故障モデル

故障は正常モデルを書き換えず、パラメータとして重ねています。

| パラメータ | 内容 | 区分 |
|---|---|---|
| 摩耗 → 自由半径増 | 摩耗 1.0 で 0.15 mm 広がる | ASSUMPTION |
| 汚れの円周分布 | 半値半幅 70°、方位 90° の raised-cosine 窓 | ASSUMPTION |
| 軸ずれ・傾きの方向 | 90°（Ring 接点側） | ASSUMPTION |
| 傾きの支点 | ジャック前面。1 次近似 | ASSUMPTION |
| 断続接触のノイズ | 深度 0.02 mm 単位のハッシュ。**時刻を使わないので再現可能**、既定は無効 | ASSUMPTION |

**軸回転について**: 理想的な同心接点ではプラグを回しても接続は変わりません。
汚れや摩耗を有効にしたときだけ回転角が効くようにしています（仕様 §5 の要求どおり）。
テストでも「汚れ 0 なら回転で結果が変わらない」ことを確認しています。

**軸ずれの誇張について**: 入口ボア φ3.6 に対しプラグ φ3.5 なので、公称どうしの片側隙間は **0.05 mm** しかありません（ボアの公差は図面から読み取れず UNKNOWN なので、実際の隙間はこれより広くも狭くもなりえます）。
プリセットの 0.25 mm は誇張表示です（UI にもその旨を明記しています）。

---

## F. TRRS（4極）— 仮定が多い領域

3極 TRS 版の検証が通ったあとに追加しました（仕様 §13）。**3極版より根拠が弱い**ので分けて書きます。

### F-1. FACT として使えたもの

独立した 2 社の図面（Same Sky SP-3544 / Cliff FC68124）で一致:

- 嵌合長 14±0.3、軸径 Ø3.5±0.05、**絶縁帯幅 0.7±0.1 が 3 か所**
- **肩基準**の寸法 **5.5±0.2** と **8.5±0.2**、および Sleeve 露出長 **2.5±0.1**
  （2026-07-31 まで「先端基準」と書いていました。誤りです → F-2）
- 先端形状（テーパ 1±0.3 / 45°±1° / R0.2±0.1、段付き径 Ø3.2 / Ø3 / Ø2.5 / Ø1）は 3極と共通
- 3極と 4極の違いは「同じ 14 mm のエンベロープ内に絶縁帯を 1 本追加して、単一リング領域を Ring1 / Ring2 に二分割している」点だけ

配列は一次資料あり: CTIA = L/R/GND/Mic（AOSP）、OMTP = L/R/Mic/GND（OMTP v1.0）。

### F-2. 導体境界 — FACT（2026-07-31 に訂正）

> **ここには誤りがありました。** 2026-07-31 まで「図面に Ring1 / Ring2 の境界寸法はありません」と
> 書いていましたが、**図面には書かれていました。** 寸法線の読み方を間違えていたためです。
> 経緯は下に残します。

図面（Same Sky SP-3544 / Cliff FC68124）の寸法チェーンは、**すべて肩を基準に先端方向へ**測られています。
`8.5±0.2` も `5.5±0.2` も、寸法線の矢印の右端が肩（φ4.5 のフランジ）で終わっています。
`14±0.3`（嵌合長）の右端と同じ位置です。

先端基準に読み替えると、記載値がそのまま導体境界になります。

| 図面記載 | 基準 | 先端基準に直すと | 意味 |
|---|---|---|---|
| 14±0.3 | 先端〜肩 | — | 嵌合長 |
| 8.5±0.2 | 肩から | 14 − 8.5 = **5.5** | Ring1 前端 |
| 5.5±0.2 | 肩から | 14 − 5.5 = **8.5** | Ring2 前端 |
| 2.5±0.1 | Sleeve の長さ | 14 − 2.5 = **11.5** | Sleeve 前端 |
| 0.7±0.1 × 3 | 絶縁帯幅 | — | 3 本とも |

```
Tip 0–4.8 │ 絶縁 4.8–5.5 │ Ring1 5.5–7.8 │ 絶縁 7.8–8.5 │ Ring2 8.5–10.8 │ 絶縁 10.8–11.5 │ Sleeve 11.5–14.0
```

合計 4.8 + 0.7 + 2.3 + 0.7 + 2.3 + 0.7 + 2.5 = 14.0 でぴったり閉じます。
**Ring1 と Ring2 は同じ幅 2.3 mm** です。これは図面記載寸法の演算なので **FACT** です。

#### 何を間違えていたか

`8.5` と `5.5` を**先端基準**だと読み、`8.5` を Ring2 の**後端**だと解釈していました。
すると Ring2 の幅が 0.7 mm（絶縁帯と同じ）、Sleeve が 4.8 mm という構成になります。

間違いに気づけなかった理由は 3 つあります。

1. **図面の `2.5±0.1`（Sleeve の長さ）を拾っていませんでした。** これを記録していれば、
   Sleeve が 4.8 mm になった時点で矛盾に気づけたはずです（[SOURCES.md](SOURCES.md) §9 も訂正済み）
2. **肩基準は自分で書いていたルールでした。** `dimensions.json` の `plug.fingerLength` の注記に
   「全ての軸方向寸法チェーンはこの肩側を基準にしている」と書き、3極側では正しく適用しています。
   **4極だけ適用し忘れました。**「確認できなかった」のではなく、既知のルールの適用漏れです
3. 誤った読みでも合計が 14.0 で閉じ、`5.5` と `8.5` という数字自体は境界に現れるため、
   検算をすり抜けました

#### 訂正で何が直ったか

誤った配置は、混挿の 3 ケースで実挙動と食い違っていました。訂正後は**6 通りすべてが一致**します。

| 組み合わせ | 訂正前 | 訂正後 | 一般に言われる挙動 |
|---|---|---|---|
| CTIA × 3極ジャック | 帰線が浮く | **正常（マイクだけ働かない）** | 普通に鳴る |
| OMTP × 3極ジャック | 正常 | **帰線が浮く（音量低下）** | 正しく鳴らない |
| 3極 × 4極ジャック | 右chが帰線に落ちる | **正常（マイク未接続）** | 普通に鳴る |

**訂正の正しさを一番強く支持しているのはこの一致です。**
誤った配置では 3 ケースが外れ、正しい配置では 0 ケースしか外れません。

### F-3. 4極ジャック — 全面的に ASSUMPTION

> **2026-08-02 に土台を入れ替えました。**この節は Same Sky SJ3-35074A の端子番号に基づく
> 旧構成の説明でした。現在は **Lumberg 1503 28**（JEITA RC-5325A）ベースです。

端子番号と軸位置（端子1=Sleeve バレル / 2=Ring2 / 3=Ring1 / 4=Tip / 5・6=ブレーク相手）は
1503 28 の図面記載で **FACT**、端子番号と機能の対応は **DERIVED** です。
**内部接点の軸位置・パッド幅・円周位置は依然としてすべて仮定**です（図面に断面図が無いため）。

**ブレーク接点は 2 個を定義しました。**1503 28 の本文 "with two break contacts" と
回路記号の両方に記載があります。開くしきい値だけは 3極側と同じ仮定を流用しています。

機器側の配線は CTIA（Android が必須とする配列）を前提にしています。


> **接点位置の仮定は、橋絡の有無には効きません。**
> 4極プラグは半径 1.750 の平坦面を 3 本持ち、その間隔は 2 つとも 0.700 mm
> （図面記載の絶縁帯幅）です。橋絡が起きる条件はパッド幅がこれを超えるかどうかだけで、
> 接点位置を成立範囲いっぱいに振ってもしきい値は **0.720 mm** から動きませんでした。
> 採用値は狭パッド 0.5（橋絡しない）／帰線パッド 0.9（橋絡する）で、
> この大小関係だけで結果が決まっています → [docs/SENSITIVITY.md](docs/SENSITIVITY.md) §4。

### F-4. 3極プラグ × 4極ジャックの結果について

本モデルは「ジャックの帰線（Ring2）接点がプラグの **Sleeve** 導体に当たり、
**ステレオ再生は正常・マイクだけ未接続になる**」と計算します。これは F-3 の仮定の直接の帰結です。

> 2026-07-31 の導体境界の訂正まで、ここには「Ring 導体に当たり、右チャンネルが帰線に落ちる」と
> 書いていました。**訂正前の結論が残っていたものです。**

この結果は、Android が定める「第3–第4セグメント間 100 Ω 未満なら 3極と判定」というしきい値の置き方
（マイク DC 抵抗の下限 1000 Ω とスピーカーインピーダンス 16〜32 Ω の中間）と整合しますが、
**実機はこの状態を検出して内部で結線を切り替えます**。
本アプリが表示しているのは**切り替え前の素の幾何結果**です。

---

## G. その他

- ジャック軸の高さ 3.0 mm … **図面記載**（側面図に軸の中心線から本体底面までの「3」がある）。ただし「基板面＝本体底面」は仮定なので、合成量としては ASSUMPTION。2026-07-31 まで「前面図で中央に描かれていることから DERIVED」と書いていたが、それは記載寸法の拾い漏れだった
- 前部ブッシング外径 6.0 mm … 表示専用だが**図面記載「ø6」**（FACT）。2026-07-31 まで 5.6 mm の推定値を置いていた
- 基板の寸法 … 表示専用
