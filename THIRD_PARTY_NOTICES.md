# THIRD_PARTY_NOTICES — 第三者資料（図面・CAD）の利用条件と商標

> 本プロジェクト自身のライセンスは [LICENSING.md](LICENSING.md) の対応表を見てください
> （コード=MIT / データ・文書=CC BY 4.0 / 同梱フォント=SIL OFL 1.1）。
> この文書は「**外部の資料をどう扱ったか**」を記録するもので、ライセンス本文ではありません。

## 結論（先に）

**メーカーの CAD・図面は、本プロジェクトに一切含まれていません。**
3D 形状はすべて、公開データシートに記載された寸法から自前で生成しています。

---

## 1. なぜ CAD を使わなかったか

Lumberg は 1503 09 / 1532 10 の STEP モデル（`.stp.gz`）と 3D PDF を、
ログインも会員登録もなしに公式サイトから配布しています。**取得自体は容易です。**

しかし Lumberg の Legal Notes は次のように定めています。

- §2 Copyrights — サイトのコンテンツを商業目的で複製・頒布・改変・第三者提供してはならない
- §5 — Lumberg Group の書面同意なく複製・頒布・再生産・再送信その他の利用は禁止。
  かつ「本ウェブサイトは Lumberg の知的財産の使用許諾を一切与えない」

**無料で取得できることは、再配布の許諾を意味しません。**
したがって、STEP を成果物へ同梱する、変換して配布する、派生 glTF を公開する、
のいずれも書面同意なしには行えないと判断しました。

（許諾を求める場合の窓口は Lumberg の marketing@lumberg.com です。）

## 2. 代替の配布サイトも検討対象から外した

TraceParts・3D ContentCentral・CADENAS (PARTcommunity / 3Dfindit)・SnapMagic といった
配布サイトからも同種の CAD を入手できますが、**いずれも再配布や改変に何らかの制約があると理解しており**、
検討対象から外しました。

> **この理解は各社規約の原文で確認したものではありません（一次未確認）。**
> 実際に利用する場合は、必ず各社の利用規約を自分で読んでください。
> 本プロジェクトは結局どのサイトからも CAD を取得していないため、この判断は成果物に影響していません。


## 3. 本プロジェクトが実際に行ったこと

| 行為 | 実施 | 備考 |
|---|---|---|
| データシート PDF の閲覧 | した | 寸法の読み取りのため |
| データシート図面のローカルでのラスタライズと寸法実測 | した | 測定結果（数値）のみを利用 |
| **STEP / 3D PDF のダウンロード** | **していない** | 内容も未確認 |
| **図面画像・CAD の再配布** | **していない** | リポジトリに存在しない |
| 公開 URL へのリンク提示 | した | 禁止対象は複製・頒布・再送信であり、リンクは規定外と解した |

寸法**値**そのものは著作物ではなく事実情報であり、出典を明示したうえで利用しています。
図面の**表現**（線画そのもの）は複製していません。

---

## 4. 本プロジェクトに含まれるアセット

| アセット | 出所 | ライセンス |
|---|---|---|
| 3D 形状（プラグ・ジャック・接点ばね） | `src/data/*.json` の寸法から実行時に生成 | 本プロジェクト |
| 寸法データ JSON | データシート記載値＋自前の実測＋明示的仮定 | 本プロジェクト（数値は事実情報、出典は SOURCES.md） |
| スクリーンショット | 本アプリの画面 | 本プロジェクト |
| フォント（HTML の UI） | ブラウザのシステムフォント | 埋め込みなし |
| フォント（3D 内テキスト） | **Noto Sans JP を 102 字にサブセットして同梱**（`public/fonts/NotoSansJP-Subset.ttf`、16.1 KB） | **SIL Open Font License 1.1**（全文を `public/fonts/OFL.txt` に同梱）。出所と再生成手順は [public/fonts/README.md](public/fonts/README.md) |
| テクスチャ・HDR 環境マップ | **使用していない** | — |

**ページ読み込み時の外部ホストへのリクエストは 0 件です。**
`drei` の `Environment` プリセット等も使っていません。

> **経緯**: `drei` の `<Text>` は内部で troika-three-text を使い、`font` を指定しないと
> 実行時に `cdn.jsdelivr.net` からフォントを取りに行きます。日本語ラベルがあるため
> CJK サブセットまで引かれ、**ページを開くだけで 17 リクエスト**出ていました
> （閲覧者の IP と User-Agent が CDN 事業者へ渡る状態）。2026-07-31 の公開前監査で実測により発見し、
> 必要な字だけを同梱して `font` を明示的に渡すよう修正しました。
>
> **検証方法**: `npm run build` した本番ビルドを `npm run preview` で配信し、
> Playwright でページ内・Web Worker 内を含む全リクエストを記録（無操作 8 秒間）。
> 修正前 17 件 → 修正後 **0 件**。開発サーバ（`npm run dev`）でも 0 件を確認しています。
> Web Worker 内の fetch はブラウザの `performance.getEntriesByType('resource')` に出ないため、
> ページ側の計測だけでは見つかりません。

---

## 5. サードパーティのソフトウェア

`package.json` の依存関係を参照してください。主なもの:

| パッケージ | ライセンス |
|---|---|
| React / React DOM | MIT |
| three | MIT |
| @react-three/fiber, @react-three/drei | MIT |
| troika-three-text（drei の `<Text>` が内部で使用） | MIT |
| zustand | MIT |
| vite, vitest | MIT |
| playwright, typescript | Apache-2.0 |

直接依存 18 件はすべて MIT または Apache-2.0 です。
ただし `npm install` で入る**依存ツリー全体**には、ほかに MPL-2.0 / BSD-2-Clause /
BSD-3-Clause / ISC / BlueOak-1.0.0 / CC0-1.0 / MIT-0 が含まれます（2026-07-31 に `node_modules` を全走査）。
**GPL / LGPL / AGPL 系は 0 件です。** いずれも本リポジトリには同梱していません。

---

## 6. 商標

「Lumberg」は Lumberg Holding GmbH & Co. KG の商標です。
本プロジェクトは Lumberg と関係がなく、同社の承認も受けていません。
部品番号の記載は、どの実在部品を参照したかを示す目的のみで使用しています。
