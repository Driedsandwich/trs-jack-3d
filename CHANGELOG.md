# CHANGELOG

このプロジェクトで**何がいつ変わったか**の記録です。
特に**訂正**（一度公開した値や主張を取り下げたもの）を省略せずに残しています。

> **なぜ残すか。** このプロジェクトは「根拠のはっきりしない数値を出さない」を方針にしています。
> その方針を守れなかった箇所こそ、読む人が知る必要のある情報です。
>
> **README にはこの履歴を置いていません。** 初見の読者にとって「以前は φ5.18 と
> 書いていました」は情報量が 0 で、受け取るのは不安だけだからです（2026-08-02 の
> 初見レビューでの指摘）。**現在の値だけを README に、経緯をここに**、と分けています。

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準じ、
バージョンは [SemVer](https://semver.org/lang/ja/) に従います。

---

## [Unreleased]

### 追加 — tag source の独立検算 helper（v0.2.0 フォローアップ §5）

`npm run verify:release-source-inputs`。**受け手がこちらの自己申告を信じずに確かめるための道具**です。
`source-input-manifest.json` の記録を tag の source と突き合わせて sha256 を計算し直します。

**v0.2.0 tag に対して 28/28 一致**しました（手元の git 経由と GitHub tarball 経由の両方で）。

**network は既定で使いません**（オーダーの要件）。`--fetch github` を明示したときだけ取りに行きます。

**「取れなかった」と「合わなかった」を潰しません。**`SOURCE_UNAVAILABLE`(2) と `MISMATCH`(1) は別で、
入力 0 件は `NOTHING_TO_VERIFY`(2) にします。**0 件を検証して OK と言うのが一番危ない**ためです。

**read-only を機械で固定**しています。書き込み API を 1 つも使わず、tar は展開せずメモリ上で読みます。

### 追加 — release index と evidence schema（v0.2.0 フォローアップ §1〜§3）

**下流が報告文から値を手で転記しなくて済むようにしました。**v0.2.0 では手入力で lock を作っており、
転記ミスの余地がありました。

`artifacts/trs-jack-3d-release-index.v1.json` を新設し、profile / 感度 / 全 asset の
`filename` / `profileId` / `inputDigest` / `sha256` / `generatedFromCommit` を持たせています。

**`artifactGenerationCommit` を 1 つにしていません。**release 工程が 2 段階なので、
profile と 感度・頑健性 は別の commit で生成されます。`artifactGenerationCommits[]` が完全な事実です。

**`releaseTag` / `releaseCommit` は既定で `null` です。**索引を作る時点では tag が存在しないためで、
分からないものを埋めません。`release:stage` は `null` のままの索引を拒みます。

evidence 3 種（validation-results / source-input-manifest / release index）に Draft-07 schema を付け、
配布物に入れました。`validate:profiles` の対象は 8 → **9 件**です。

`validation-results.json` の各対象へ `distribution`（`RELEASE_ASSET` / `SOURCE_ONLY`）を追加しました。
**9 対象のうち 2 件は配布しません。**受け手が「全対象を bundle だけで独立再検証できる」と読まないためです。

### 破壊的変更 — 頑健性の窓の端点（`topology-robustness` schemaVersion 1 → 2）

| 旧 | 新 |
|---|---|
| `fromMm` | **`startMm`** |
| `toMm` | **`lastSampleMm`** |
| （無し） | **`endExclusiveMm`** |

**旧 `toMm` は「最後に当たった標本の位置」で、区間の終端ではありませんでした。**
profile の `nominalEndMm`（13.52 mm）と 1 刻みずれて見え、**同じ語で 2 つの違う量を指していました。**

項目名を変えるのは破壊的変更なので `schemaVersion` を上げ、`contractMigration` も持たせました。
据え置いたまま名前を変えると、読む側は `undefined` を受け取り**沈黙して壊れます**
（v0.1.0 → v0.1.1 の `spreadStatus` で実際に起きた型）。

### 訂正 — 索引が「一つ前の検証結果」を指していた

索引を `validation-results.json` より先に書いていたため、その sha256 と `generatedFromCommit` が
古いままでした。生成順を入れ替えて直しました。**生成物を数えて気づいたもので、
「動いた」だけでは出てきませんでした。**

### 破壊的変更 — Schema v2（非阻害フォローアップ P1-5 / P2-6）

**v1 とは非互換です。**v1 を期待する実装は `schemaVersion` を見て停止してください。
対応表は profile の `contractMigration` にあります。

| | 旧 | 新 |
|---|---|---|
| `topologyClass` | `fully-seated` | **`all-expected-functions-match`** |
| profile のファイル名 | `half_plug_topology_profile.v1.*` | **`half_plug_topology_profile.v2.*`** |
| `schemaVersion` | 1 | **2** |

**なぜ `schemaVersion` を上げたのか。**v0.1.0 → v0.1.1 で `spreadStatus` の enum を
非互換に変えたのに `schemaVersion` は 1 のままにしました。その結果、下流の adapter は
`spreadStatus !== 'MEASURED'` で全 event を弾き、**エラーも警告も出さずに汚染検出が
丸ごと素通り**しました。**沈黙が最悪の壊れ方です。**

**方式は実測で選びました。**下流が何を見るかを確かめたところ、

- `schemaVersion` を 2 にする → `Unsupported profile schemaVersion: 2` で**停止する**
- `contractRevision` を足すだけ → **どこも読まないので PASS する**

沈黙を避けるという目的に対して、答えは 1 つしかありませんでした。

**`fully-seated` を改めた理由。**「プラグ肩が当たった」と読めますが、実体は `reasonCode` の
とおり `ALL_EXPECTED_FUNCTIONS_MATCH` でしかありません。名前を直すだけでは同じ誤読が
起きるので、**その差を数字でも出します**（`mechanicalInsertion.gapMm`。TRS×TRRS で 0.48 mm）。

**`normalized` の射程を機械可読にしました。**`normalizedScope: PROFILE_LOCAL` /
`crossProfileComparable: false`。文章では v0.1.1 で弱めていましたが、
**機械が読める形では何も言っていませんでした。**

### 追加 — release evidence の自己完結性（非阻害フォローアップ P2-7 / P2-8）

v0.1.1 の配布物では、受け手が次を確かめられませんでした。どれも「こちらは知っているが
渡していない」だけだったので、渡します。

| 追加した asset | 何が確かめられるようになるか |
|---|---|
| `artifacts/validation-results.json` | こちらで意味規則が通っているか（判定は `validate:profiles` と同一実装） |
| `artifacts/source-input-manifest.json` | `provenance.inputFiles[].sha256` を tag source と独立に検算できる |
| `artifacts/test_counts.json` | **tag 時点の件数**（v0.1.1 では入っておらず、報告の 260 件が tag の 258 件か main かを判別できなかった） |
| `schemas/event-sensitivity.v1.schema.json` ほか | 感度・頑健性 artifact を受け手が検証できる（v0.1.1 では**入れ忘れていた**） |

asset の一覧は `scripts/releaseAssets.mjs` に固めました。**その場で選ぶのをやめます。**
入れ忘れは「毎回思い出す」に頼っていたから起きました。

`package.json` の `version` も配布版と揃えました（v0.1.1 tag では `0.1.0` のままで、
release tooling の判定材料にできませんでした）。

### 訂正 — `validate:profiles` が symlink 経由だと何も検証せず成功していた

CLI 判定をパスの単純比較で書いていたため、symlink 越しに起動すると `main()` が走らず、
**出力ゼロ・終了コード 0** になりました。realpath で比べるよう直しました。
変異試験を symlink 構成で回して見つけたもので、**このリポジトリが一番嫌う壊れ方**です。

### 訂正 — TRS の感度解析が TRS×TRRS profile へ混入していた（統合フォローアップ P0-1〜P0-4）

**Half-Plug Lab 側の fixture import で見つかった。**こちらの検査 45 本は 1 つも捕まえなかった。

`scripts/sensitivity.ts` は解析基準を `TRS|JACK-TRS` に固定している。
ところが exporter は variant を問わず単一の `artifacts/sensitivity.json` を読み、
その `eventSpread.byKind` を**どの profile へも配っていた。**

```
TRS×TRS   FIRST_BREAK_OPEN  名目 8.06mm  幅 8.06〜8.06mm   一致
TRS×TRRS  FIRST_BREAK_OPEN  名目 8.48mm  幅 8.06〜8.06mm   **名目値が幅の外**
```

名目値が自分の幅の外にあるのは、その幅が別のモデルのものだからである。

**2026-08-03 の P0-3 修正は浅かった。**「kind 単位の集計を事象へ配るな」は直したが、
「**別 variant の解析を流用するな**」は見落としていた。同じ根の、より深い層である。

#### 直したこと

- `scripts/sensitivityEvents.ts` を新設し、**variant ごとに** event spread を測る
  （`artifacts/sensitivity.<slug>.json`）。`variantId` / `analysisScope` /
  `sweptParameters` / `generatedFromCommit` を記録する
- exporter は **`variantId` が profile と一致しなければ取り込まない（fail-closed）**。
  一致しなければ `spreadMm: null` / `spreadStatus: NOT_ANALYZED` / `available: false`
- 3極の総合解析（プラトー間隔・Tip 橋絡しきい値・挿抜力）は**3極 variant でのみ**出力する
- `sweptParameters` を直書きせず artifact の記録から取る
  （直書きだと、値が別 variant のものでも気付けない）
- **`inputDigest` を variant 別にした**（P1-2）。3極の感度を測り直しただけで
  4極 profile の ID まで変わっていた

実測: TRS×TRRS の `FIRST_BREAK_OPEN` の幅は **7.82〜8.64 mm**（3極は 8.06〜8.06）。
名目 8.48 はこの中に入る。振った寸法も `trrs.jack.contact.*` になった。

**破壊的変更**: `spreadStatus` を改名した。`MEASURED` は実物測定と誤認される。

| 旧 | 新 |
|---|---|
| `MEASURED` | `MODEL_SWEEP_EVENT_SPECIFIC` |
| `NOT_EVENT_SPECIFIC` | `MODEL_SWEEP_NOT_EVENT_SPECIFIC` |
| `NOT_MEASURED` | `NOT_ANALYZED` |

### 追加 — 記録された値どうしの整合を検査する（11 件）

**候補 12 件を変異させたところ、11 件が素通りした。**
意味規則を 45 本書いていたが、構造（一意性・連続性・存在）ばかりで、
**値どうしの整合をほとんど見ていなかった。**

追加したもの:

- **`spreadStatus` が幅を持つなら、名目値がその幅の中にある**（今回の欠陥そのもの）
- `normalizedEnd` が mm と合う（`normalizedStart` しか見ていなかった。単なる非対称）
- 事象の `normalized` が `depthMm` と合う／深さ順に並ぶ／範囲内にある
- 区間の `evidenceGrade` がその区間の接点の最弱と一致する
- `assumptionSummary.counts` が台帳と一致する
- `absentTopologies.searched` が実際に現れたクラスを覆っている
- **`inputFiles[].sha256` が実ファイルと一致する**（provenance の話は全部これに乗っているのに、
  一度も検査していなかった）

見送ったもの: `contacts` と `terminalToPlugNet` の突き合わせ（ブレーク接点を含む
回路グラフ経由なので単純な同一性にならず、誤検出になる）、
`stepMm` と区間幅の関係（丸めで脆い）。

### 訂正 — 自前の schema 検証器が `pattern` を実装していなかった（統合オーダー P0-6）

2026-08-03 まで JSON Schema の検証を自前で書いていた。実装していたのは
`required` / `type` / `enum` / `const` / `additionalProperties` / `$ref` /
`minItems` / `minimum` の 8 種で、**`pattern` が無かった。**

ところが同じ日の P0-1 で、provenance に `pattern` 制約を **5 本**足していた
（`inputDigest` の sha256、`generatedFromCommit` の 40 桁 hex、`artifactDate` の
日付形式、`inputFiles[].sha256`）。**そのどれも検証されていなかった。**
「schema に書いたから守られている」と報告したが、実際には素通りしていた。

実測: 意図的な違反 10 種のうち **5 種が自前の検証器を素通り**した（ajv は全部検出）。

> **現物の artifact に違反は 0 件だった。**誤った値が公開されたことはない。
> 守りが効いていなかっただけである。

### 追加 — `npm run validate:profiles`（ajv / draft-07）

- `ajv` ^8.20.0 を **devDependencies のみ**に追加（実行時依存には入れない）
- 対象を **5 件**へ広げた（profile 2 件 / 探索 / 実部品比較 / テスト件数）
- 新規 schema 3 本: `topology-search.v1` / `real-jack-comparison.v1` / `test-counts.v1`
- **schema 検証と意味検証を分けた。**落ちたときにどちらの種類か即座に分かる
  （形の問題と中身の問題では直し方が違う）
- 意味規則は 45 本。区間の連続性、`intervalId` / `eventId` の一意性、
  `sourceRevision` が 40 桁 hex か `UNKNOWN` か、`inputDigest` が sha256 か、
  **`profileId` の末尾が `inputDigest` の先頭 12 桁と一致するか**、
  生成物自身が入力に混ざっていないか、反対証拠が消えていないか
- `npm run test` からも回る（別コマンドにしか無いと回し忘れても緑になるため）

変異試験: 意味規則 45 本のうち **44 本に個別の変異を当て、全部が狙った規則を発火**させた。
残る 1 本は 44 本目の鏡像で、単独では発火しない（無害な重複）。

> 検証器そのものが自分の記録に引っかかった。「廃止した名前が復活していないか」を
> 全文検索で見ていたため、`removedMeasures` に残した廃止記録を違反として拾った。
> 記録を除いて検索するよう直した。

### 訂正 — 型検査を 3 セッション「何も検査しない形」で回していた

`npx tsc --noEmit -p tsconfig.json` を使っていたが、`tsconfig.json` は
`"files": []` と `references` だけの**振り分け専用**なので、
**対象 0 件で正常終了していた。**本物は `npm run typecheck`（`tsc -b`）である。

そのあいだ、テストもビルドも通っていた。**vitest も vite も型検査をしない。**
実際に**未定義の関数を呼ぶコードが 1 コミット分すり抜けており**
（`scripts/searchTopology.ts` の `classifyFromEvaluation`）、
`npm run search:topology` を実行して初めて `ReferenceError` で落ちた。
同時に、前コミットの `scripts/provenance.ts` に未使用 import が残っていたことも判明した。

**過去の報告にある「typecheck rc=0」のうち、この期間のものは根拠になっていない。**
再発防止は CONTRIBUTING §7 に書いた（検査は必ず package.json の script 名で呼ぶ）。

### 訂正 — 電気トポロジーの分類が 5 か所に散っていた（統合オーダー P0-4）

「帰線が浮き、L と R が別々の導体に届いている」という同じ判定が、
`circuit.ts` / `searchTopology.ts` / `compareRealJack.ts` とテスト 2 件の
**計 5 か所**に書かれていた。`src/model/topology.ts` の
`classifyElectricalTopology()` を唯一の正本にし、5 か所すべてを向けた。

指摘どおり、`searchTopology.ts` の説明文が旧実装のままだった
（「判定順の都合で L と R が同じ導体でも `GROUND_OPEN` になる」）。
**その挙動は 2026-08-02 に直っている。**逆向きの陳腐化として削除し、テストで固定した。

一本化して初めて分かったことが 2 件ある。

- **同じ数を 2 つの名前で報告していた。**`usableWitnesses` と
  `strictDifferenceSignal` はどちらも 1,338 件だった。目標が「厳密な差分信号」
  そのものになった時点で後者は定義上すべて前者に一致する。廃止し、
  廃止したこと自体を artifact の `removedMeasures` に残した。
- **短絡の種類を取り違えていた。**`shortsSignalToSignal` を
  「`TIP` と `RING` に同時接触」という**導体名**で判定していた。
  導体名は位置であって機能ではない（OMTP では Ring2 と Sleeve の機能が入れ替わる）。
  分類器へ差し替えた結果、profile に `signal-to-signal-short` と `on-insulator` が
  現れるようになった（従来はどちらも `signal-to-return-short` へ丸められていた）。

**破壊的変更**: `topologyClass` を `acousticAnnotation` から
`electricalTopology` へ移した（電気的な事実と聴感の仮説を別層にする）。

### 訂正 — 「作れる」「市販品のまま」を名乗っていた（統合オーダー P0-5）

| 旧 | 新 |
|---|---|
| `realizablePadWidth` | `passesPadWidthHeuristic` |
| `needsNoModification` | `matchesCurrentNominalParameters` |

0.3 mm という閾値に**出典が無い**ことを `heuristic.source: null` /
`manufacturingVerified: false` として機械可読にした。
材料・ばね応力・耐久性・成形・公差・接触圧・メーカー工程のいずれも確認していない。

README の見出しを「半挿しにすると、**無改造で**左右差分が残ります」から
「半挿しで左右差分が残る区間は、**モデル上の候補です**」へ直し、
**同じ表の直下に反対証拠**（PS000001 では区間 0 件）を置いた。

### 追加 — artifact provenance（統合オーダー P0-1）

profile に「何から作られたか」を残した。**`sourceRevision` では固定できなかった。**

監査時の HEAD は `ba58b4c`、コミット済み profile は `sourceRevision: 5adf454` で、
その間にモデルデータが変わっていた。かといって `sourceRevision === HEAD` は要求できない。
**artifact を含めてコミットすると HEAD が変わるので自己参照になる**（生成した瞬間に
正しかった値が、コミットした瞬間に「古い」と判定される）。

そこで**入力ファイルの中身だけ**を指紋にした（`provenance.inputDigest`、21 ファイル）。

| したこと | `generatedFromCommit` | `inputDigest` |
|---|---|---|
| 寸法を 1 文字直した（未コミット） | 変わらない | **変わる** |
| artifact だけ作り直してコミットした | **変わる** | 変わらない |

**破壊的変更**: `profileId` の作り方を
`trs-jack-3d:<variant>:<revision 12桁>` から
`trs-jack-3d:<variant>:<inputDigest 12桁>` へ変えた。
revision 版は中身が同じでもコミットのたびに ID が変わり、
逆に寸法を直しても未コミットなら ID が変わらなかった。どちらも誤りだった。

- `SOURCE_REVISION` の素通しを廃止（実際の HEAD と食い違えば止まる。
  意図する場合だけ `--unsafe-revision-override`）
- `--release` は入力が dirty なら生成を拒否する
- `workingTreeDirty` は**入力ファイルだけ**を見る（木全体を見ると、artifact を
  書き出した直後は必ず dirty になり、clean な入力からでも release を作れなくなる）
- 受入試験 7 項目を `test/provenance.test.ts` と `npm run verify:provenance` に置き、
  **7 項目すべて変異試験で落ちることを確かめた**

> **リポジトリにコミットされている profile は `artifactKind: "local"` /
> `workingTreeDirty: true`** である（開発中に生成しているため）。
> 正本にできるのは clean な入力から `--release` で作った release asset だけで、
> それは P0-8（承認待ち）で作る。

### 訂正 — 公開済み profile の 4極ジャック根拠が古かった（統合オーダー P0-2）

2026-08-02 に 4極ジャックを Lumberg 1503 28 ベースへ組み直したとき、
**`scripts/exportHalfPlugProfile.ts` の直書き文字列だけが取り残された。**
`artifacts/half_plug_topology_profile.v1.trs_jack_trrs.json` は
その後（`sourceRevision: 1f26460`）作り直されているのに、次の記述が残っていた。

```
source: 一次資料なし
note:   4極ジャックは接点位置を含めて全て仮定である。図面もデータシートも入手できていない。
```

**実際には端子 6 本の軸位置とブレーク接点 2 個は図面記載（FACT）で、
仮定として残っているのは接点の軸方向オフセット 1 つだけである。**

さらに `test/halfPlugProfile.test.ts` が

```ts
expect(trrsProfile.jackBasis.note).toMatch(/接点位置を含めて全て仮定/)
```

と書いており、**テストが古い主張を守る側に回っていた。**
語句を固定するのをやめ、「強い根拠と弱い根拠が分けて書かれていること」を固定する形へ改めた。

- 根拠は台帳の区分から組み立てる（`detail` に 7 項目）。台帳を直せば artifact が追随する
- `modelLimitations` を variant ごとに書き分けた（従来はどの variant でも「1532 10 × 1503 09」）
- 反対証拠（PS000001 で区間が消えること）を `jackBasis.note` に残した

> **これは「軽い成果物の陳腐化 0 件」の反例ではない。**作り直しは行われていた。
> 陳腐化していたのは**生成器が持っていた文章**で、`inputs` の記録では検出できない種類である。

### 訂正 — `events[].spreadMm` が事象固有でなかった（統合オーダー P0-3）

感度解析の `kind` 単位の集計を全事象へ複製していた。
`STATE_CHANGE` は 1 回の挿入で **29 件**出るため、29 件すべてに同じ
`−0.88〜14 mm`（＝挿入ストローク全域）が付き、
**Ring のブレーク接点にも帰線接点用の幅が付いていた。**

- `kind` が 1 回しか出ない事象にだけ幅を付ける（`MEASURED` 7 件 / `NOT_EVENT_SPECIFIC` 29 件）
- `kind` 単位の集計は `sensitivitySummary.aggregateSpreadByKind` へ移した（捨てていない）
- `eventId` を導入。`label` の文言からは作らない
- `docs/HALF_PLUG_ADAPTER.md` §4 に、意味の変更と移行方法を書いた

> **一意性の検査が、書いた直後に前提の誤りを捕まえた。**
> 帰線接点は絶縁帯を 2 本またぐので同じ遷移が 2 回起きる。
> `kind:subject:from->to` だけでは **7 件が衝突**していた。

### 追加 — `npm run check:vacuity`（空振りしているテストの検出）

**通っているテストが何も検査していない**欠陥を、2026-08-02〜03 に 7 回作った。
`test/` 全体を機械で洗うと候補は **142 か所**あったが、
1 件ずつ見ると**本当に偽の合格になるのは 7 か所**だった（誤検出 95 %）。

構文を洗うのはやめ、**結果の側**（skip されたか／件数が減ったか）を見る検査にした。

実測（成果物を 5 件消して走らせた結果）:

| ファイル | 修正前 | 修正後 |
|---|---|---|
| `docs.test.ts` | ENOENT で落ちる（**ガード無しが正しい**） | 変更なし |
| `topologySearch.test.ts` | 12 件が黙って skip | ENOENT で落ちる |
| `realJackComparison.test.ts` | 6 件が黙って skip | ENOENT で落ちる |
| `uiStrings.test.ts` | 3 件が黙って合格 | ENOENT で落ちる |

変異試験で、`realJackComparison.test.ts` の
**「図面値では左右差分の区間が出ない」（反証を守るための検査）が、
端子の引き方を壊しても通る**ことも確認した。
`differenceWindowCount` が「引けなかった」を **0** として返していたためで、
`test/_must.ts` の `mustFind` で即座に落とすようにした。

書き方は [CONTRIBUTING.md §7](CONTRIBUTING.md) に残した。

### 判断 — `check:stale` を全成果物へは広げない

軽い成果物 7 件（`events` / `contact_sweep` / `force_curve` / `variant_matrix` /
`verification_summary` / profile 2 件）にも入力の記録を持たせる設計を検討したが、**採らなかった。**

| | |
|---|---:|
| 古いまま残った実績（直近 14 コミットの実測） | **0 件** |
| 作り直す手間 | **数秒**。1 コマンドでまとめて回せる |

重い 2 つ（10〜15 分）とは事情が違う。代わりに **`npm run artifacts:all`** を用意し、
「困ったら全部回す」で足りる形にした。

### 訂正 — artifact とモデルを突き合わせるテストが 1 つも無かった

検討の途中で、**本当の穴**が見つかった。

- `docs.test.ts` … 文書 ↔ artifact
- `contact.test.ts` / `trrs.test.ts` … モデルの挙動
- **artifact ↔ モデル … 誰も見ていなかった**

つまり `npm run artifacts` を回し忘れると、**古い artifact に文書が合っている**ので
すべて緑のまま、公開している数値だけが実際のモデルとずれる。
代表値をモデルから計算し直して突き合わせるテストを 2 件足して塞いだ
（`events.json` の主要イベント 4 種と、`verification_summary.json` の区分件数）。

> **その初稿で自分も空振りを作った。**`BRIDGE_START` / `BRIDGE_END` という
> **実在しない kind 名**を並べ、3 件中 2 件が `null === null` を比べていた。
> このリポジトリで 6 度目の同型である。実在を先に検査するガードを入れ、
> 変異（`FIRST_BRIDGE` の深さを書き換える）で落ちることを確認した。


### 確認 — CONTRIBUTING §3 の手順を初めて通しで実行した

**「手順を書いた」と「手順が通ることを確かめた」は別**なので、2 種類の変更で実際に通した。

| | 変えたもの | 結果 |
|---|---|---|
| A | 表示専用の `note` を 1 行足す | artifact 差分ゼロ・テスト全通過。**手順 2〜3 が空振りになるのが正しい挙動** |
| B | 帰線パッド幅 0.9 → 0.92 | artifact 5 件が変化し、**手順 5 のテストで 3 件が期待どおり落ちた** |

**手順は機能した。**連動漏れは `npm run test` で捕まる。

### 訂正 — 重い成果物の再実行条件が足りていなかった

CONTRIBUTING §3 には「接点位置や区分を変えたときは `search:topology` と `sensitivity` も必要」と
書いていた。**この条件では足りない。**上の Drill B で変えたのは**パッド幅**だが、
両方のスクリプトがパッド幅を走査軸に持っている。手順書からはそう読み取れなかった。

**条件を人が覚えるのをやめ、機械判定に置き換えた。**

- `npm run check:stale` を新設。**成果物自身が記録している実行時の値**と現在のモデルを突き合わせる
  （`topology_search_*.json` は軸ごとの `shipped`、`sensitivity.json` は新設の `inputs`）
- キーの一覧を人が保守しないので、**走査軸が増えても勝手に追随する**
- 変異で検出を確認済み（パッド幅を 0.92 にすると、キー名と必要なコマンドを名指しで出す）
- `sensitivity.json` は schemaVersion 4 → **5**（`inputs` を追加）。
  解析結果そのものは不変で、差分は +14/−1 行だった（決定性の確認にもなった）

判定に必要な記録が消えると、**静かに「再実行不要」と答えてしまう**ので、
記録の存在を検査するテストを 2 件置いた。


### 追加 — `CONTRIBUTING.md`（不整合 5 種類・計 25 件の記録と、守り方の基準）

2026-08-02〜03 に見つけた不整合を 1 枚にまとめた。**5 種類とも根は 1 つで、
「モデルを直したあと、それを説明している場所を全部は直せていない」**である。

| 種類 | 件数 | 守り方 |
|---|---:|---|
| (a) 文書の数値が artifact と食い違う | — | 機械照合 68 件 |
| (b) 画面の文字列が古い | 5 | 不変条件 6 件（台帳方式は採らない） |
| (c) 逆向きの陳腐化（解決済みを未確認と言い続ける） | 6 | 個別テスト 5 件（語句スキャンは採らない） |
| (d) 根拠区分が実態より強い | 12 | 不変条件 3 件 |
| (e) 同一文書内で値が割れる | 2 | 個別テスト 2 件（構造変更はしない） |

**「守らない」判断も理由つきで残した。**残さないと同じ検討をやり直すことになる。
置き場は `docs/` ではなく root の `CONTRIBUTING.md` にした。
これは記録であると同時に「変更する前に読むもの」だからである。

### 追加 — テスト件数の機械照合（唯一、手で直し続けていた連動）

直近 8 コミットで `dimensions.json` と同時に変わったファイルを実測したところ、
根拠区分表も「ジャック内部の仮定件数」も**既に機械照合されていた**。
手作業が残っていたのは**テスト件数だけ**で、README / UNKNOWNS / `docs/TEST_RESULTS.md` の
3 か所を毎回書き写していた（8 コミット × 3 か所 ＝ **24 回**）。

- **README と UNKNOWNS からは数を消した。**読者にとって「212」と「213」の違いに意味はない
- **`docs/TEST_RESULTS.md` には残した。**そこでは件数が報告の内容そのもの
- `npm run test:count` が `artifacts/test_counts.json` を作り、`docs.test.ts` が突き合わせる

> **書いた直後に設計の欠陥を踏んだ。**テストが 1 件でも落ちると `execFileSync` が例外になり、
> **件数を更新できない → その 1 件が落ち続ける**という循環になった。
> vitest は失敗してもレポートを書くので、例外を握って続行し、成否は `allPassed` に記録する形へ直した。


### 点検 — 文書どうしの食い違いを 6 主張 × 7 文書で洗い、1 件だけ見つかった

前回、README の不変量リストがすぐ下の自分の注記と矛盾していた。同じ形が他にもないか、
**橋絡の深さ / Tip↔Ring が橋絡しないこと / ブレーク接点 / 4極ジャックの接点位置の根拠 /
挿抜力の校正 / 完全挿入深度 14mm** の 6 主張を、7 文書で突き合わせた。
該当行は **257 行**あったが、**値が割れていたのは 1 件**だった。

**ブレーク接点が開く深さ** … README と REPORT §4 は **8.06**、
SENSITIVITY と REPORT §5 は **8.05**。

artifact で確かめると:

| 出所 | 方法 | 値 |
|---|---|---|
| `events.json` | 0.02 mm 刻みの走査 | **8.06**（9.44 / 11.24） |
| `sensitivity.json` | **二分法** | **8.0509**（9.43 / 11.23） |

**同じ切り替わりを別の方法で測った値**で、どちらも正しい。
橋絡については同じ説明（二分法 11.760 と走査 11.78）が既にあるのに、
ブレーク接点には無かった。3 文書に方法の違いを書き添えた。**値は変えていない。**

### 判断 — 「1 つの主張は 1 か所」への構造変更はしない

1 件しか出なかったので、参照へ置き換える構造の見直しは割に合わない。
方法の違いを書き添え、artifact から両方の値を読んで固定する回帰テストを 2 件置いた。

### 一致していたもの（記録）

Tip 橋絡の余裕倍率（1.19 / 2.38 / 3.08）は 5 文書で一致。橋絡の幅レンジ 6.96〜13.14 mm、
完全挿入深度 14.0 mm も一致。ピーク挿入力に残る **6.97 N** は
「面取りがあった頃の値」という**訂正の記録**で、現在値の主張ではなかった。


### 変更 — README 冒頭の理由づけを、件数から「確か／不確か」の切り分けへ

根拠区分の見直しで ASSUMPTION が 43 → 54 件（寸法 135 件の 40 %）へ増えたが、
冒頭の理由は**件数だけ**だった。件数は「どれくらい信用できないか」を表さない。
このアプリは**順序と仕組みには頑健で、深さと力の値だけが仮定に乗る**。
読者が最初に決める分岐（使ってよいか）に効く形へ書き換えた。

| | |
|---|---|
| **確か** — 仮定を振り直しても変わらない | 接点が触れる**順序**／その過程で Ring↔Sleeve の橋絡が起きること |
| **不確か** — 仮定に直接乗る | **何 mm で**起きるかという深さ／挿抜力の絶対値／ブレーク接点がいつ開くか |

数値は増やさず、件数は 1 か所（`ASSUMPTIONS.md` への導線）に集約した。

### 訂正 — 押付力の根拠が「別メーカーの資料」だと書いていなかった

制約の節は「摩擦係数とばね定数は**メーカー公称レンジ**に整合するよう置いた仮定値」と
書いていた。**この押付力レンジ（0.8〜1.3 N/接点）は TE Connectivity の資料**で、
モデル化した Lumberg 1503 09 のものではない。`SOURCES.md` §6 は
「この値を 1503 09 の仕様として表示してはいません」と定めているのに、
README だけ同じ部品の公称値のように読める書き方になっていた。

### 訂正 — 「順序は変わらない」にブレーク接点を含めていた

README は不変量として「ブレーク接点が正しい結線より先に開くこと」を挙げていたが、
**すぐ下の注記自身が「しきい値 0.4 以上では一度も開かない」と書いていた。**
不変量の一覧から外し、「不確か」の側へ移した。


### 訂正 — 根拠区分を 92 件点検し、11 件を DERIVED から ASSUMPTION へ下げた

前回、`jack.housing.axisHeightFromPcb` が「図面記載の 3.0」＋「本体底面＝基板面という仮定」の
合成量なのに FACT になっていた。同じ形が他にもないか、**FACT 53 件と DERIVED 39 件の
計 92 件を 1 件ずつ**点検した。

| 区分 | 前 | 後 |
|---|---:|---:|
| FACT | 53 | **53**（変更なし） |
| DERIVED | 39 | **28** |
| ASSUMPTION | 43 | **54** |

下げた 11 件:

- **接点ばねの自由半径・公称たわみ・ばね定数 9 件**（tip / ring / sleeve × 3）。
  完全挿入時の押付力が**別メーカー資料**（TE Connectivity 108-19423）の 0.8〜1.3N/接点 に
  収まるよう逆算した値で、記載寸法からの演算でも図面実測でもない。
  `ASSUMPTIONS.md` A 節が冒頭で「**内部接点ばねの寸法は公開資料に含まれていません**」と
  書いているのに、A-3 だけ DERIVED のまま残っていた（A-1 の軸位置は 07-31 に同じ理由で下げ済み）。
  同じ A-3 の中で許容たわみだけが ASSUMPTION だったのも、その取り残しである。
- `jack.entryBushingLength`（3.5）… ノーズ長の全長がボアだという仮定。
  `UNKNOWNS.md` §3-8 が「**DERIVED の仮定**」という自己矛盾した書き方をしていた。
- `plug.handle.bodyDiameter`（10.0）… 実測は対辺 φ10.56 / 対角 φ11.43 の八角形で、
  φ10 は表示用の丸め。表示専用。

**値は 1 つも変えていない。**区分だけである。

### この点検で分かったこと

- **FACT 53 件からは 1 件も出なかった。**FACT の付け方（一次資料に数値として直接記載）は
  守られている。ズレは「導出したつもりで、実は置いていた」DERIVED 側に集中していた。
- `plug.tip.end`（Tip 導体と第1絶縁帯の境界 4.8）は**据え置いた**。
  解釈を含むが、図面実測の分割線と 2 社図面の絶縁帯幅という 2 つの裏付けがあり、
  `ASSUMPTIONS.md` B-1 が理由付きで DERIVED と判断している。新しい証拠なしに覆さない。
- 再発防止のテストを 3 件置いた（`test/uiStrings.test.ts`）。
  とくに「接点まわりで DERIVED を名乗れるのは図面の幾何から出るものだけ」は、
  許される 3 件を名指しで固定しているので、増えたら落ちる。


### 訂正 — 「逆向きの陳腐化」を洗い、6 件を直した

前回、4極 CTIA プラグの説明文が「Ring1/Ring2 の境界は仮定」と書いていたのを見つけた。
**その境界は 2026-07-31 に FACT へ解決済み**で、「まだ分かっていません」と言い続けるほうの
陳腐化だった。同じ形が他にもないか全体を洗い、**6 件**見つけた。

| # | 場所 | 何が古かったか |
|---|---|---|
| 1 | `dimensions.json` `jack.housing.axisHeightFromPcb` | **grade が FACT なのに、note 自身が「依然として未確認」と書いていた** |
| 2 | `docs/REPORT.md` | 「Ring1 / Ring2 の境界は仮定」（07-31 に FACT へ解決済み） |
| 3 | `ASSUMPTIONS.md` G | 軸高さの導出が、記載寸法の拾い漏れ前の説明のまま |
| 4 | `UNKNOWNS.md` §2 | 「JEITA が読めれば Ring1/Ring2 境界を確定できた」（もう確定済み） |
| 5 | `ASSUMPTIONS.md` F-3 | 端子番号の出典が Same Sky SJ3-35074A のまま（現在は Lumberg 1503 28） |
| 6 | `ASSUMPTIONS.md` F-3 | 「4極ジャックのブレーク接点は定義していません」（08-02 に定義済み） |

**1 番は区分の誤りである。**図面が与えるのは「軸の中心線から本体底面まで」で、
「本体底面＝基板面」は未確認の仮定。合成量なので弱いほうに合わせて **ASSUMPTION** へ下げた
（FACT 54→53 / ASSUMPTION 42→43）。表示専用で接触判定・回路・力には入らない。

### 判断 — 汎用の語句スキャンはテストにしない

`dimensions.json` の 135 キーに「grade が FACT なのに note に『仮定/不明/未確認』」
「grade が ASSUMPTION なのに note に『図面記載/FACT』」という語句スキャンをかけると
**9 件**引っかかるが、**実質は 1 件**だった（誤検出 8/9 = 89 %）。
誤検出は「歴史的な記述」と「同じ note の中の別の量を指している」の 2 種類で、
語句スキャンは文脈を見られない。恒久的なノイズになるので採らず、
**見つかった 6 件を名指しで固定する**回帰テストを 5 件置いた（`test/uiStrings.test.ts`）。

`docs.test.ts` が「artifact の全数値を突き合わせる」のをやめて主張の台帳にしたのと同じ判断である。


### 訂正 — 画面に出る文字列を洗い、5 件のズレを直した

前回、4極ジャックの説明文が組み直し前のまま（Same Sky 準拠）で嘘になっているのを
**偶然**見つけた。文書の数値は `test/docs.test.ts` が artifact と機械照合しているが、
**画面の文字列は誰も見ていなかった。**そこで全数を洗い出した。

母集団は **JSON 側 238 件（`_comment` を除き、実際に描画されるフィールドのみ）＋
UI 直書き 160 件**。実際に陳腐化していたのは **5 件**。

| # | 場所 | 何が嘘だったか |
|---|---|---|
| 1 | `App.tsx` ヘッダ | 構成モデルを選んでも「実在する代表的な 3.5mm TRS 接続の一例」と名乗っていた |
| 2 | `materials.json` | 「ローレットナット (CuZn ニッケルめっき)」が 4極でも出る。1503 28 のノーズは樹脂 |
| 3 | `dimensions.json` ボア径 | 17mm プラグ用の別規格品（Cliff FC68128）だけを出典に挙げていた |
| 4 | 4極 OMTP プラグの説明文 | 構成モデルであることをどこにも書いていなかった |
| 5 | 4極 CTIA プラグの説明文 | 「Ring1/Ring2 の境界は仮定」と書いていた。**その境界は 2026-07-31 に FACT へ解決済み** |

**5 番は逆向きの陳腐化である。**「まだ仮定です」と言い続けるほうも嘘になる。
最初に書いたテストは「仮定」という語の有無しか見ておらず、これを素通りさせた。

あわせて、前回 `manufacturer` に説明文を入れてしまいヘッダが読めない文になっていたのも直した
（説明は variant の `description` が持つ。識別子の欄は識別子に保つ）。

### 判断 — 画面文字列に「台帳方式」は入れない

`docs.test.ts` と同じ台帳（主張と期待値の対応表）を 398 件に対して作るのは割に合わない。
文言を直すたびに台帳も直すことになり、しかも「台帳を通したから正しい」という
誤った安心を生む。**5 件とも「これはどの実在部品を表しているか」という同じパターン**だったので、
そのパターンだけを突く不変条件を 6 件置いた（`test/uiStrings.test.ts`）。

- 識別子の欄（`partNumber` / `manufacturer`）に説明文を入れない
- 構成モデルを含む組み合わせでは「実在の一例」と名乗らない
- 構成モデルの説明文は「実在の特定製品ではない」と明言する
- 解決済みの項目を「仮定」と言い続けていない
- 表示される出典 ID がすべて出典表に実在する
- 材質ラベルが、どの部品のものかを名乗っている

**モデルの値は 1 つも変えていない。**すべて表示専用の文言修正である。


### 判断 — 4極ジャックの外形は揃えない（3極 1503 09 の流用のまま）

接点系は 1503 28 へ移したが、外形は移さないことにした。図面からは読めている
（全長 14.00 / ノーズ 2.00 × ø5.00 / 本体 7.50 × 4.20 / SMT ミッドマウント）。
それでも揃えなかった理由は 4 つ。

1. **`axisHeightFromPcb` が読み取れない。**1503 28 はミッドマウント品で基板が本体の途中を
   通るため、3極品のような「基板面から軸まで」という量にならない。正面図の 4.20 と 1.50 から
   どちらとも読めた。**根拠タブに区分つきで出る数値を推測で埋めるわけにはいかない。**
2. **寸法だけ入れ替えると嘘が増える。**3D は前面の円柱を「ローレットナット
   (CuZn ニッケルめっき)」として描くが、1503 28 のノーズは樹脂。材質は variant 別に
   切り替えられる構造になっていない。「1503 28 の形をした金属ナットつきの何か」になる。
3. 端子の形が違う（SMT パッド 6 本 2 列 / スルーホール 5 本 1 列）。
4. **外形は接触判定・回路・力のどの計算にも入らない。**揃えても結論は 1 つも変わらない。

読み取った外形寸法は `SOURCES.md` §13 に記録した（将来やるときのため）。
判断の詳細は `docs/REAL_JACK_COMPARISON.md` §8、未確認事項としては `UNKNOWNS.md` §5-6。

### 訂正 — 画面の variant 説明文が組み直し前のままだった

4極ジャックの説明文に「端子番号は Same Sky SJ3-35074A に準拠」が残っていた。
前回の組み直しで Lumberg 1503 28 式へ変わっており、**画面に出る文言が嘘になっていた。**
文書は直したのに画面を直していなかった形で、**画面のほうが読まれる**ぶん害が大きい。

1503 28 ベースであること・接点位置は仮定のままであること・**外形は流用であること**を
説明文に入れ、3 件のテストで固定した（説明文を元に戻す変異で 2 件が落ちることを実測）。


### 変更 — 4極ジャックを Lumberg 1503 28 ベースへ組み直した

それまでの 4極ジャックは接点位置 4 件が完全な架空値だった。
**Lumberg 1503 28**（JEITA RC-5325A 準拠・ブレーク接点 2 個・SMT）の図面へ土台を移した。

| 項目 | 前 | 後 |
|---|---|---|
| 端子 6 本の軸位置 | 定義なし | **FACT** 4.74 / 4.74 / 7.50 / 11.30 / 12.50 / 13.50 |
| 端子番号 ↔ 機能 | Same Sky SJ3-35074A 式 | **1503 28 式**（DERIVED） |
| ブレーク接点 | 定義なし | **FACT** 2 個 |
| 接点の軸位置 | 4 件を独立に選んだ仮定 | **仮定は 1 つだけ**（`beamOffset`・採用値 0・成立範囲 0〜1.3mm） |
| 本命の区間 | `IV019` 12.90〜13.12mm | **`IV028` 13.30〜13.52mm** |

**§5-2 は閉じていない。**1503 28 の図面にも断面図が無く、接点そのものの位置は
どこにも書かれていない。**「実在部品ベースになった」は「実測された」ではない。**
残る仮定 `beamOffset` は 0〜1.3 のどこでも区間が出るので、
**「起きるかどうか」は仮定に依らず、「何 mm で起きるか」だけが依る。**

**やらなかったこと:** 外形（ハウジング・ノーズ・ナット）は 3極 1503 09 の値を流用したまま。
1503 28 は SMT のミッドマウント品でナットを持たないので、3D 表示の外形は実物と違う。
接点系だけを実在部品に合わせた合成であり、1503 28 の再現ではない。

### 新しい結果 — 4極プラグ × 4極ジャックでも起きるようになった

組み直し前、この組み合わせは**成立 0 件**で、README にも「4極 CTIA × 4極ジャックでは 0 件」
と書いていた。Lumberg の端子位置を入れると **深さ 12.79〜13.00 mm で成立する**。

| variant | 前（成立 / 無改造） | 後（成立 / 無改造） |
|---|---|---|
| 3極プラグ × 3極ジャック | 78 / 0 | 78 / 0 |
| **4極 CTIA × 4極ジャック** | **0 / 0** | **396 / 1** |
| 3極プラグ × 4極ジャック | 162 / 1 | **864 / 1** |

**マイク付きイヤホンをスマホのジャックへ半挿しにする**という、
3極プラグを使うより普通の状況である。残る仮定 `beamOffset` を 0〜1.3 のどこに置いても成立する。

（成立数が全体に増えているのは、下の「無改造を評価していなかった」訂正で
走査水準に既定値を足し、探索空間が 14,670 → 24,750 構成へ広がったため。）

### 解決 — 4極ジャックのブレーク接点（UNKNOWNS §5-3）

`BRK_TRRS_RING`（端子3↔5）/ `BRK_TRRS_TIP`（端子4↔6）をモデルへ入れた。
開くしきい値だけは 3極側と同じ仮定を流用している（図面に動作量の記載が無いため）。

### Half-Plug へ渡す profile — `schemaVersion` は 1 のまま

項目・型・意味を変えていないため。変わったのは**データ**である。
ただし `intervalId` は区間の並び順から機械的に振った番号なので、
**データが変わると同じ番号が別の状態を指す**（実際に `IV019` → `IV028`、区間総数 21 → 30）。
`HALF_PLUG_ADAPTER.md` §4 に、`profileId` と組で保存し、
違っていたら `topologyClass` と `normalized` で引き直すよう明記した。

### 訂正 — 構成探索が「無改造で成立: 0」と誤報していた

既定値が動いたのに、振る水準の直書きリストが古いままだったため、
**「無改造の構成」が一度も評価されないまま 0 と報告された。**
エラーは出ず正常終了する形の誤りで、このリポジトリで 5 度目の同型である。
水準に既定値を必ず混ぜる `withShipped()` と、入っていなければ落ちる
`assertShippedIncluded()` を入れた。

### 訂正 — テストが端子 ID を直書きしていた

`test/trrs.test.ts` が `terminalToPlugNet['P2']` のように端子 ID を直書きしており、
端子番号が Same Sky 式から Lumberg 式へ変わった際に 3 件落ちた。
**ID 直書きは 2026-08-02 の探索事故で一度禁じた形**だったが、このファイルに残っていた。
`signalRole` で引く形へ直した。


### 追記 — Lumberg の 4極品を読んだら、逆の結論が出た

Lumberg 自身の 4極対応組み合わせ（プラグ **1532 02** / ジャック **1503 28**、
どちらも JEITA RC-5325A 準拠）のデータシートを取得して読んだ。

| 根拠 | Tip 接点の軸位置 | 左右差分の区間 |
|---|---:|---|
| 本モデルの仮定 | 11.4 | 12.89〜13.11 |
| pro-SIGNAL PS000001（**接点**の寸法記入） | 12.75 | **なし** |
| Lumberg 1503 28（**端子**位置） | 11.30 | 13.30〜13.52 |

**実在資料 2 件が逆を指している。**証拠の種類が違う（一方は断面図の接点そのもの、
他方は基板レイアウトの端子）ため、資料だけでは決まらない。
直前の版で「唯一の実在図面に支持されていない」と書いたが、
**正確には「実在資料 2 件が割れている」**。README・UNKNOWNS・
REAL_JACK_COMPARISON の §0 を訂正した。

- **1532 02 には導体境界の寸法が無かった。** 3極の 1532 10 が持つ `8.5`/`5`/`0.7`
  に相当する記載がなく、独立した 3 社目の裏付けにはならない。図面実測では構成が一致し
  合計 14.000mm で閉じるが、線幅 0.239mm のため精度 ±0.12mm で、0.7 と 0.6 を区別できない。
- **1503 28 の端子位置 3 本が、接点が入るべき範囲に全部入る**（Ring2 3.2〜5.5 に 4.74 /
  Ring1 6.2〜8.5 に 7.50 / Tip 9.2〜14.0 に 11.30）。Cliff FC68125 のような端子逆順では
  こうならないので、この部品は接点が端子のほぼ真上にあると読める。

### 解決 — 4極ジャックのブレーク接点（UNKNOWNS §5-3）

「実在品にあるかどうか未確認」としていたが、**Lumberg 1503 28 が本文に
"with two break contacts" と明記し、回路記号も 3極の 1503 09 と同じ構造を示していた。**
本モデルの 4極ジャックは依然としてブレーク接点を持たない（入れるなら 1503 28 ベースへ組み直す）。

### 判断 — 4極モデルを Lumberg 1503 28 ベースへ組み直す価値はある（未実施）

端子位置 6 本・ブレーク接点の構成・ボア径が FACT になり、端子番号↔機能が DERIVED になる。
**接点位置は ASSUMPTION のまま**だが、自由に選んだ値から実在部品の端子位置に拘束された値へ格上げされる。
影響範囲は実装 11 ファイル・artifact 4 件・文書の 4極数値 16 箇所で、
Half-Plug へ渡した `IV019` の値も動くため、**この移行だけを独立した変更として扱う**
（→ docs/REAL_JACK_COMPARISON.md §7）。


### 訂正 — 「3極プラグ×4極ジャックで左右差分が残る」は、実在図面 1 件に支持されていない

4極ジャックの接点位置には一次資料が 1 つも無く、4 件とも仮定でした。
これを資料で埋められないか **13 方面・170 型番**を実際に開いて探し、
**接点位置そのものが寸法記入された図面を 1 件**見つけました（pro-SIGNAL PS000001）。

その値を入れると、**看板だった左右差分の区間が消えます。**

| | Tip 接点の軸位置 | 区間 |
|---|---:|---|
| 本モデルの仮定 | 11.4 mm | 12.90〜13.12 mm |
| PS000001 の図面値 | **12.75 mm** | **出ない** |

4 値のうち 3 値はほぼ一致しました（差 0.03 / 0.05 / 0.20 mm）。
**外れたのは Tip の 1 件だけで、その 1 件が結論を決めていました。**
成否の分かれ目は **Tip 接点が 12.6 mm より浅いこと**で、これは今回まで書かれていなかった条件です。

**取った対応:**

- 図面値へ**差し替えていません**。図面にあるのは接点位置 4 件だけで、
  パッド幅・ばね・端子配置は無いためです（理由 → [docs/REAL_JACK_COMPARISON.md](docs/REAL_JACK_COMPARISON.md) §4）
- **variant も足していません**。同じ理由で、部品の形をした別物になります
- 代わりに `npm run compare:real-jack` と `artifacts/real_jack_comparison.json` で固定し、
  **この結果を消す変更が入ったらテストが落ちる**ようにしました（`test/realJackComparison.test.ts` 10 件）

### 追加

- `docs/REAL_JACK_COMPARISON.md` — 突き合わせの結果と、**探した範囲の記録**（13 方面 170 型番の内訳）
- `SOURCES.md` §12 pro-SIGNAL PS000001 / §13 Lumberg 1503 28（どちらもモデルには未使用）

### 分かったこと（今後の指針）

- **端子の位置から接点の位置は出せません。** 端子位置ならどのメーカーでも手に入りますが、
  実在の 4極品では**端子の並びが接点の並びと逆**です（Cliff FC68125: Tip 端子 3.4 / Sleeve 端子 12.4 mm）。
  3極の Lumberg 1503 09 で使った読み方は、4極には一般化できません。
- **Lumberg は 4極の対応組み合わせ（1503 28 / 1532 02）を持っています。**
  3極と同じ品質で 4極を組み直せる道です。ただし断面図は無く、接点位置は依然として仮定になります。

### 訂正 — Cliff FC68128 は 17mm プラグ用だった

`SOURCES.md` §9 で挿入口径 Ø3.6 の根拠として引いていた FC68128 は、
図面タイトルが **"3.5mm PHONE JACK - 4 POLE (17mm PLUG)"** で、嵌合長 17 mm の別規格品でした。
口径の根拠としては有効ですが、14 mm プラグの部品として扱ってはいけません。注記を追加しました。

---

## [0.1.0] — 2026-08-02

**最初のタグ付きリリース。**Half-Plug Lab との統合契約が固まったので版を固定します。
以降、破壊的な schema 変更は v2 へ上げます。

### 追加

- **`docs/HALF_PLUG_ADAPTER.md`**: Half-Plug 側 adapter へ渡す仕様。
  初期マッピング（1 対 1 ではない）、`ground-open-differential` を自動で
  L−R 係数へ変換してはいけない理由、本命区間 `IV019`、
  0.2 mm の窓は機構ではなく UI で解く、を明記。
- **profile を variant ごとに出せるようにした**（`--variant`）。
  無改造で左右差分が残るのは `TRS|JACK-TRRS` だけなので、そちらも成果物に加えた。
- **判定の分離**: 帰線が浮いた状態を 2 つに分けた。
  `DIFFERENCE_SIGNAL`（L と R が別導体 → 左右差分が残る）と
  `GROUND_OPEN`（L と R が同じ導体 → 差分にならずほぼ無音）。
  型に定義済みで一度も生成されていなかった `DIFFERENCE_SIGNAL` を使っている。
- **目標トポロジー探索**（`npm run search:topology`）。
  既定モデルに現れない共通帰線断が、**構成を変えれば成立するのか**を調べる。
  14,670 構成中、完全挿入を壊さず本命の状態を通るのは **240 件**。
  ただし全て `constructed`（実在部品ではない）で、帰線パッドを既定の 9 分の 1 にする等が要る。
  **4極 CTIA × 4極ジャックでは 0 件。**
- **Half-Plug 連携用の中立エクスポート契約**（`schemas/half-plug-topology-profile.v1.schema.json` /
  `scripts/exportHalfPlugProfile.ts` / `artifacts/half_plug_topology_profile.v1.json`）。
  ※このファイル名は後日 variant 別（`...v1.trs_jack_trs.json` 等）へ変わりました。
  接点トポロジーを DSP から独立した JSON として出す。
  **音響係数ではありません** — `acousticAnnotation` は参考分類です。
- `CHANGELOG.md`（このファイル）

### 変更

- README を初見レビューの結果にもとづき再構成（→ [2026-08-02](#2026-08-02)）

### 訂正 — 「左右が同じ導体に落ちていても『左右差分が残る』と表示していた」

`predictAcoustic` の帰線断の判定が左右短絡の判定より先にあり、しかも両者を
区別していなかった。**L と R が同じ節点に落ちていて差分が生じない場合まで
「左右差分が残る」と表示していた。** 構成探索で 318 件中 78 件しか本当の
差分信号でなかったことから見つかった。下流で回避せず、発生源を直した。

### 訂正 — README の「帰線パッドを 9 分の 1 にする等が要る」は言い過ぎだった

3極×3極で選んだ 1 例の話を、成立条件一般のように書いていた。
パッド 0.3 mm 以上に限っても 240 件中 **159 件**が残る。
さらに **3極プラグ × 4極ジャックは無改造で成立する**（深さ 12.9〜13.1 mm）。
市販の 4極ジャックへ 3極イヤホンを半挿しにするだけで、特別な部品を要さない。

ただし 4極ジャックの接点位置は一次資料が無く全て仮定なので、
「既定値のまま起きる」は「仮定した位置のまま起きる」以上の意味を持たない。
現象の存在は仮定の振り方に頑健（成立する working 構成の 38 %）だが、
深さの数字は仮定に完全に依存する。

### 訂正 — 探索の実装で、同じ形の空振りを 4 回作った

いずれも**例外を出さず、静かに間違った数字を返す**種類の誤りだった。

| 何を持ち込んだか | 症状 |
|---|---|
| 4極ジャックへ 3極の**寸法キー** | override が効かず、2 variant が既定値のまま数百回繰り返された |
| usable と broken を**混ぜて上位 50 件** | 「318 件ある」と書きながら 1 件も収録しない artifact ができた |
| 4極ジャックへ 3極の**端子 ID** | 厳密判定が常に false。**78 件と報告していたが実際は 240 件** |
| variant を**混ぜて上位** | 240 件と書きながら 3極しか収録せず、4極の可否が判断できなかった |

前 2 者は「3極の前提を 4極へ持ち込んだ」形、後 2 者は「異質なものを混ぜて上位 N 件」の形。
**2 番目を直した直後に 4 番目を作っており、同型と気づかずに再発させた。**

対策として、探索開始前に落ちるガードを 2 つ（`assertAxesBite` /
`assertRolesResolvable`）置き、標本は種類別・variant 別に層化した。
`test/topologySearch.test.ts` は**「探した」という主張そのもの**を検査する。

---

## 2026-08-02

### 変更 — README を初見の読者の指摘で再構成

経緯を知らない読者 4 人（うち 1 人はセッション上限で未完）に読ませ、
2 人以上が独立に挙げた点を直しました。

- **「使ってよいか」を 338 行目から 8 行目へ。** 否定だけでなく**使える用途**も併記。
  読者が最初に決める分岐なので、338 行かけて答えを出すのは読者コストの浪費でした。
- **深さの「幅」を主表の前へ。** 精密な数字を先に見せて後から幅を出す順序だったため、
  流し読みすると 11.78 mm を確定値として持ち帰る構造でした。
- **深さ表の全行に幅の列を追加**（測っていない行は「—」と明示）。
- **二分法の値を表の外へ。** 6.96〜13.14 mm の幅を持つ量に 4 桁を併記すると
  精度の印象が戻る、と 2 人が独立に指摘したため。「真値」という語も、初学者には
  実物の値に読めるので使うのをやめました。
- **用語表を追加。** TRS が Tip / Ring / Sleeve の頭字語であることがどこにも
  書かれておらず、学生の読者は「別物だと思って読んでいた」。
  絶縁帯・橋絡・ブレーク接点・パッド・プラトー・compliance も未定義でした。

### 訂正

- **「±3 mm 動きます」は実測と合っていませんでした。** 実際は非対称で最大 4.82 mm
  （橋絡 −4.82 / +1.36、ブレーク接点 −0.06 / +4.26）。
  **対称な誤差だという誤った印象**も与えていました。artifact を見ずに丸めたのが原因です。
- **「ジャック内部の寸法は 37 件の仮定」の 37 は、台帳のどの部分集合でもありませんでした。**
  実測は jack.\* = 14 / jack + trrs.jack = 20 / 全体 = 41。単に古い数字でした。
  同じ README の別の場所には「ASSUMPTION 41」とあり、**仮定件数が 2 通り併存**していました。
- `UNKNOWNS.md` の単体テスト件数が 69 件のまま放置されていました。
- **中心的主張の余裕が、README では実際より強く見えていました。**
  「1/3.1」しか書いておらず、追従量の採用**範囲** 0.02〜0.10 という概念が出てこない。
  範囲上端と公差の最悪コーナーを組み合わせると余裕は **1.19 倍**しか残りません。

---

## 2026-08-01

### 追加 — 感度解析

仮定を成立範囲いっぱいに振り、何が不変で何が動くかを実測（`docs/SENSITIVITY.md`）。
結論は「**仕組みは揺れない、深さの数字は揺れる**」。

- 文書の数字と生成物を機械照合するテストを追加（`test/docs.test.ts`）。
  変異テストで 47 件すべてが落ちることを確認済み。
- `dimensions.json` に `sweepRange` を新設。`tolerance` は左右対称の公差しか
  表せず、実測が片側にしか散っていない量に使うと UI が誤表示するため。

### 訂正 — 感度解析の初版に手法上の欠陥が 4 つあった

独立した反証で、初版の主張 6 件のうち **3 件が覆りました**。

- **看板の証拠が空でした。**「compliance < 0.15 の 729 通りで Tip 橋絡 0 件」は、
  水準の取り方の誤りで**すべて compliance = 0 の一点**。そこは Tip に一切導通できず
  完全挿入すら成立しない退化モデルでした。
- 基準値 11.77 → **11.760**（走査格子由来の値だった）
- パッド幅しきい値 0.721 → **0.720**（0.001 刻みの目盛りだった）
- 橋絡深さの幅 9.0〜13.1 → **6.96〜13.14**（片振りの合併だった）
- **「2 つの FACT から出る幾何的帰結」を撤回。** φ3.20 は DERIVED です。
  正しくは「**公称寸法のモデルでは**起きない」。

作り直した版でも**単調性を仮定した二分法**というバグを踏みました
（橋絡もブレーク接点も深さに対して単調ではない）。

### 訂正 — 看板の表に、どの生成物にも無い深さが載っていた

README と REPORT の表の **8.22 mm** は、`events.json` にも `sensitivity.json` にも
`contact_sweep.json` の変化点にも存在せず、幾何から計算しても出ませんでした。
正しくは **8.26**。出典を `artifacts/events.json` と明記した表の中で、
**由来を追えない値が公開されていました**。

`docs/TEST_RESULTS.md` §5 の描画コスト 4 値も artifact のどの値とも一致せず、
測定日も 1 日ずれていました（fps 表だけが再測定で更新された部分更新）。

---

## 2026-07-31

### 訂正 — 存在しない面取りを削除

導体と絶縁帯の境界に置いていた **0.09 mm の面取り**は、図面を 1200 dpi で
ラスタ化したときに**線の太さ（0.302 pt ≒ 0.107 mm）を読み違えた**ものでした。
ベクタで見ると各段はちょうど 1 本の垂直線で、面取りの作図も寸法記載もありません。

- 接触・導通・橋絡の結論は**不変**（イベント深度のずれは最大 0.035 mm）
- ポリゴン数 6,228 → **5,460**（差 768 = 6 点 × 64 分割 × 2 三角形）
- **挿抜力モデルは壊れました。** 段が幅ゼロになるとたわみが不連続になり、
  ランプ項が発散します。上限（`force.maxRampSlope`）を明示的な仮定として追加。
- **ピーク挿入力 6.97 N は、存在しない面取りの傾きが作っていた値**でした。現在は 5.27 N。

### 訂正 — 一次資料の読み落とし

**「図面に記載が無い」と書いていたもののうち 3 件は、こちらの読み落とし**でした。

| 項目 | 訂正前 | 訂正後 |
|---|---|---|
| ジャックのナット径 | 5.6 mm（注記は図面と正反対のことを書いていた） | **ø6**（図面記載・FACT） |
| ジャック軸の高さ | 「特定できていない」 | **図面が直接寸法していた**（FACT） |
| 歪み止めの外径 | 台帳に無く φ10 → φ5.8 の円錐で描いていた | **断面 A-A に Ø7**（FACT） |

4極プラグの Ring1 / Ring2 境界も同様で、**寸法線を先端基準だと読み違えて**いました。
訂正の結果、混挿 6 通りすべてが一般に言われる挙動と一致するようになりました。

> **原因**: `pdftotext` で拾えないことを「記載が無い」の根拠にしていました。
> CAD の寸法テキストは輪郭線としてストロークされているため、テキスト抽出には出てきません。

### 訂正 — 接触判定がたわみ後の高さを見ていなかった

接点が届いているかを「たわむ**前**の自由半径」で判定していました。
押し開かれた後の位置で見直していなかったため、**第1絶縁帯でも橋絡すると計算していました**
（8.28 mm）。修正後、第1絶縁帯の橋絡は消え、第2絶縁帯の Ring↔Sleeve だけが残ります。

- プラグ肩の外径 φ5.18 → **φ10**（φ5.18 に当たる造形は図面のどこにもありませんでした）

### 追加

- 公開前監査（`docs/PUBLISH_AUDIT_20260731.md`）。high 14 件をすべて解消。
- MIT ライセンスの public リポジトリとして公開。
  実名を含む古いコミットが SHA 指定で取得できる状態だったため、**履歴は引き継がず**、
  現在の内容だけを単一のコミットとして新しいリポジトリへ置いています。

---

## 2026-07-30

### 追加 — 初版

- 3D ビューア（TypeScript / React 19 / Vite / Three.js / React Three Fiber）
- 決定論的な接触判定・回路グラフ・挿抜力モデル
- 全寸法に根拠区分（FACT / DERIVED / ASSUMPTION / UNKNOWN）と出典
- 4極 TRRS（CTIA / OMTP）プリセットと混挿計算
- 故障モード 12 種
