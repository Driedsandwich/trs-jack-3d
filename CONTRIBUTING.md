# 変更するときに読むもの

> この文書の HTML 版（`docs/CONTRIBUTING.html`）は `npm run docs:html` で生成しています。**HTML を直接編集しないでください。**

このリポジトリは「根拠のはっきりしない数値を出さない」を方針にしています。
そのため**モデルを直すより、モデルを説明している場所を直すほうが手間がかかります。**

2026-08-02〜03 に不整合を 5 種類・**計 25 件**見つけました。
**5 種類とも根は 1 つで、「モデルを直したあと、それを説明している場所を全部は直せていない」**です。
以下はその記録と、いま何が機械で守られているかの一覧です。

---

## 1. 見つかった 5 種類

| 種類 | 件数 | 例 | いまの守り方 |
|---|---:|---|---|
| **(a) 文書の数値が artifact と食い違う** | — | 「±3 mm 動きます」（実際は非対称で最大 4.82 mm）／「37 件の仮定」（台帳のどの部分集合でもなかった） | **機械照合**（`test/docs.test.ts` の 68 件。artifact から期待値を組み立てる） |
| **(b) 画面の文字列が古い** | **5** | 4極ジャックの説明文が「Same Sky SJ3-35074A に準拠」のまま／ヘッダが構成モデルでも「実在する代表的な一例」と名乗る | **不変条件 6 件**（`test/uiStrings.test.ts`）。台帳方式は採らない → §2 |
| **(c) 逆向きの陳腐化** | **6** | 「Ring1/Ring2 の境界は仮定」（07-31 に FACT へ解決済み）／「4極ジャックのブレーク接点は定義していません」（08-02 に定義済み） | **個別テスト 5 件**。汎用の語句スキャンは採らない → §2 |
| **(d) 根拠区分が実態より強い** | **12** | 接点ばねの自由半径・ばね定数が DERIVED（実際は別メーカー資料への逆算）／`axisHeightFromPcb` が FACT（note 自身が「未確認」と書いていた） | **不変条件 3 件**。とくに「接点まわりで DERIVED を名乗れるのは図面の幾何から出るものだけ」は許される 3 件を名指しで固定 |
| **(e) 同一文書内で値が割れる** | **2** | README の不変量リストがすぐ下の注記と矛盾／REPORT が 8.06 と 8.05 を併記 | **個別テスト 2 件**。1 件しか出なかったので構造変更はしない → §2 |

> **(a) だけ件数を書いていないのは、機械照合を入れる前に見つけた分を数え直していないからです。**
> 現在は 68 件の照合が入っており、変異テストで全件が落ちることを確認しています。

---

## 2. 守り方を選んだ基準 — **先に数えてから決める**

守り方を増やす前に、**実際に何件あったかを数えます。**
このリポジトリでは、次の 3 回とも「数えたら、重い仕組みは要らなかった」になりました。

| 場面 | 母集団 | 実際のズレ | 採った守り方 |
|---|---:|---:|---|
| 画面の文字列 | 398 件（JSON 238 + UI 直書き 160） | **5** | 台帳ではなく**不変条件 6 件**。398 件の台帳は文言を直すたびに台帳も直すことになり、「台帳を通したから正しい」という誤った安心を生む |
| 根拠区分の note 矛盾 | 135 キー | **1**（語句スキャンは 9 件ヒット＝**誤検出 89 %**） | 語句スキャンは**採らない**。文脈（歴史的記述／同じ note 内の別の量）を見られないため恒久的なノイズになる |
| 文書どうしの食い違い | 6 主張 × 7 文書＝該当 257 行 | **1** | 「1 つの主張は 1 か所に書いて他は参照する」への**構造変更はしない**。方法の違いを書き添えるだけ |

**守らないという判断も記録します。**そうしないと、同じ検討を次の人がやり直すことになります。

---

## 3. モデルを変えたら、どこが連動するか

直近 8 コミットで `src/data/dimensions.json` と**同時に変わったファイル**の実測です。

| 連動先 | 誰が守っているか |
|---|---|
| `artifacts/verification_summary.json` の区分件数 → README / REPORT の根拠区分表 | **機械照合**（`docs.test.ts`） |
| 「ジャック内部の寸法は N 件の仮定を含み」（README 2 か所） | **機械照合**（`docs.test.ts`） |
| `artifacts/half_plug_topology_profile.v1.*.json`（区分は接点から伝播する） | **機械照合**（`halfPlugProfile.test.ts`） |
| `artifacts/sensitivity.trs_jack_*.json`（走査軸に載っている寸法を変えたとき） | **機械判定**（`check:stale` が `provenance.inputDigest` で照合。→ 下） |
| `artifacts/topology-robustness.trs_jack_trrs.json`（接点位置・プラグ導体境界を変えたとき） | **機械判定**（同上） |
| 接点まわりの区分 | **不変条件**（`uiStrings.test.ts`） |
| 画面の variant 説明文・材質ラベル | **不変条件**（`uiStrings.test.ts`） |
| **テスト件数**（README / UNKNOWNS / `docs/TEST_RESULTS.md`） | **機械照合**（`artifacts/test_counts.json` と突き合わせ。→ §4） |

### 手順

1. `src/data/*.json` を直す
2. `npm run artifacts` — 走査結果と区分件数を作り直す
3. `npm run export:half-plug:all` — profile を作り直す（区分が変わったときは必須）
4. **`npm run check:stale`** — 重い成果物の再実行が要るかを**機械が判定します**（→ 下）
5. `npm run test` — **ここで連動漏れが落ちます**
6. `npm run test:count` — テスト件数の artifact を更新する
7. **`npm run check:vacuity`** — 空振りしているテストが無いかを機械が判定します（→ §7）
8. **`npm run validate:profiles`** — 成果物 9 件を schema と意味規則で検証します
   （2026-08-03 に感度 2 件・頑健性 1 件・入力一覧 1 件を追加。それまで schema が無く、受け手が独自に構造検査を書いていました）
9. `npm run docs:html` — HTML を作り直す

> **release を作るときは、さらに 2 つ回します。**
> `npm run release:evidence`（検証結果と入力一覧を artifact にする）→
> `npm run release:stage -- --version vX.Y.Z`（asset を集めて `SHA256SUMS` を作る）。
> **asset の一覧は `scripts/releaseAssets.mjs` が正本です。**その場で選ばないでください——
> v0.1.1 では選び忘れて `event-sensitivity` schema を落とし、受け手が感度 artifact を
> 検証できませんでした。`release:stage` は `artifactKind: local` の artifact を既定で拒みます。

> **`npm run verify:provenance` は手順に入れません。**
> これは clean checkout から生成して provenance を確かめるもので、
> **開発中は必ず落ちます**（入力を直している最中なので作業ツリーが汚れている）。
> 回すのは release を作る前だけです（→ §9・P0-8）。

### 重い成果物（10〜15 分）は、必要かどうかを機械に訊く

`npm run search:topology`（約 10 分）、`npm run sensitivity`（約 15 分）、
`npm run search:robustness`（5,184 構成。1 構成 137ms の実測から約 12 分の見積り）は毎回は回せません。
かといって回し忘れると**成果物だけが古い値のまま残ります。**

**条件を覚えないでください。`npm run check:stale` が判定します。**

```
$ npm run check:stale
  照合: topology_search_difference_signal.json: 軸 11 件

**再実行が必要です。**

  npm run search:topology -- --target DIFFERENCE_SIGNAL
    topology_search_difference_signal.json: jack.contact.sleeve.padWidth が 0.9 → 0.92 に変わっている
```

### 軽い成果物は「全部回す」で足ります

`npm run artifacts` が作る 5 件（`events` / `contact_sweep` / `force_curve` /
`variant_matrix` / `verification_summary`）と profile 2 件は、**数秒でまとめて作り直せます。**
そのため入力の記録（重い 2 つの `shipped` / `inputs`）は**持たせていません。**

- **実測: 直近 14 コミットで、これらが古いまま残った例は 0 件**でした
- 一括で回す入口があります → **`npm run artifacts:all`**
  （`artifacts` → `export:half-plug:all` → `compare:real-jack` → `test:count`）

> **ただし「回し忘れても緑になる」穴が 1 つありました。**
> `docs.test.ts` は**文書 ↔ artifact** を見ており、**artifact ↔ モデル**を見るテストが
> ありませんでした。古い artifact に文書が合っていれば、全部緑のまま公開値だけがずれます。
> 2026-08-03 に、代表値をモデルから計算し直して突き合わせるテストを 2 件足して塞ぎました。

判定は**成果物自身の記録**で行います。`topology_search_*.json` は走査軸ごとに `shipped`（実行時の値）を、
`sensitivity.json` は `inputs`（走査に使った寸法値）を持っています。
現在のモデルと突き合わせるだけなので、**走査軸が増えても勝手に追随します。**
人がキーの一覧を保守しません。

> **2026-08-03 まで、ここには「接点位置や区分を変えたときは必要」と書いていました。
> これは条件として足りていませんでした。**下の通し確認で、
> 帰線パッド幅を変えたときも両方の再実行が要ることが分かったためです。

---

## 4. テスト件数だけは、ずっと手で直していました

2026-08-03 までの 8 コミットすべてで、テスト件数を **README / UNKNOWNS / `docs/TEST_RESULTS.md` の
3 か所**手で直していました。**24 回の手作業**です。

- **README と UNKNOWNS からは数を消しました。**読者にとって「207 件」と「212 件」の違いに
  意味はなく、手作業を増やすだけでした。
- **`docs/TEST_RESULTS.md` には残しました。**そこでは件数そのものが報告の内容だからです。
  代わりに `npm run test:count` が `artifacts/test_counts.json` を作り、
  文書の数字と機械照合します。

---

## 5. 手順を実際に通した記録

**「手順を書いた」と「手順が通ることを確かめた」は別です。**通した記録を残します。

### 2026-08-03 — 初回の通し確認

2 種類の変更で、上の手順だけを順に実行しました。

| | 変えたもの | 結果 |
|---|---|---|
| **A** | 表示専用の `note` を 1 行足す（`jack.nutDiameter`） | artifact 差分ゼロ・テスト 213 件すべて通過。**手順 2〜3 が空振りになるのが正しい挙動** |
| **B** | 帰線パッド幅 0.9 → 0.92（接触判定に効く） | 手順 2 で `events.json` / `contact_sweep.json` / `force_curve.json` が変化、手順 3 で profile 2 件が変化、**手順 5 のテストで 3 件が期待どおり落ちた**（README と REPORT の深さが `events.json` に無い、走査値と真値の取り違え） |

**手順は機能しました。**連動漏れは手順 5 で捕まります。

**ただし 1 つ穴がありました。**B で変えたのは「接点位置」でも「区分」でもなく**パッド幅**ですが、
`search:topology` も `sensitivity` も**パッド幅を走査軸に持っています**。
当時の条件（「接点位置や区分を変えたときは必要」）からはそう読み取れませんでした。
そこで `npm run check:stale` を作り、条件を機械判定に置き換えました。

> 検出器そのものも試しました。パッド幅を 0.92 にすると
> **キー名と必要なコマンドを名指しで出す**ことを確認し、戻すと消えることも確認しています。

### 2026-08-03 — `check:stale` を全成果物へ広げるか検討した

**広げませんでした。**軽い成果物 7 件に入力の記録を持たせる設計も検討しましたが、

| | |
|---|---|
| 古いまま残った実績 | **0 件**（直近 14 コミットの実測） |
| 作り直す手間 | **数秒**。しかも 1 コマンドでまとめて回せる |

重い 2 つ（10〜15 分）とは事情が違います。代わりに **`npm run artifacts:all`** を用意し、
「困ったら全部回す」で足りる形にしました。

**その代わり、本当の穴のほうを塞ぎました。**回し忘れを検出する仕組みが無くても、
`artifact ↔ モデル` を突き合わせるテストがあれば、ずれたまま緑にはなりません。

## 7. 空振りしないテストの書き方

**通っているテストが、何も検査していないことがあります。**
このリポジトリでは 2026-08-02〜03 に **7 回**、同じ形の欠陥を作りました。
どれも「識別子で引く → 見つからない → それでもテストが通る」です（一覧は `test/_must.ts`）。

### 数えた結果 — 構文で洗うのは筋が悪い

`test/` 全体（当時 217 件）を機械で洗うと、候補は **142 か所**ありました。
1 件ずつ見ると、**本当に偽の合格になるのは 7 か所**でした（**誤検出 95 %**）。

| 形 | 候補 | 実際に空振りする | なぜ |
|---|---:|---:|---|
| `.find(...)!`（非 null 断言） | 41 | **0** | 引けないと `undefined.x` で **TypeError** になり、落ちる |
| `for..of`（空集合で素通り） | 87 | **0** | 空になるのはモデルが壊れたときで、それは他の数十件が先に捕まえる |
| `describe.skipIf` / 早期 `return` | 6 | **6** | **静かに消える。** 落ちも通りもしない |
| 引けなくても値を返す関数 | 5 | **1** | `?? []` が「0 件」と「引けなかった」を同じ値にする |

**構文ではなく「miss が偽の合格になるか」だけが境目です。** 落ちるなら直しません。

### 実測した被害

成果物を 5 件消して走らせました（`artifacts/` は 14 件とも git 管理下なので、本来ありえない状態です）。

| ファイル | 結果 |
|---|---|
| `docs.test.ts` | **ENOENT で落ちた。** ガードを持っていない。**これが正しい挙動** |
| `topologySearch.test.ts` | 12 件が**黙って skip** |
| `realJackComparison.test.ts` | 6 件が**黙って skip** |
| `uiStrings.test.ts` | 3 件が**黙って合格**（早期 `return`） |

**ガードが働くのは「壊れたときに黙る」ときだけでした。**
`skipIf` と早期 `return` を全部外し、`docs.test.ts` と同じく素直に読むようにしました。

さらに変異試験で、`realJackComparison.test.ts` の
**「図面値では左右差分の区間が出ない」（反証を守るための検査）が、
端子の引き方を壊しても通る**ことを確かめました。
`differenceWindowCount` が「引けなかった」を **0** として返していたためです。

### 検査コマンドそのものが空振りすることもある

**2026-08-03、型検査を 3 セッションにわたって「何も検査しない形」で回していました。**

```
$ npx tsc --noEmit -p tsconfig.json    # ← rc=0。しかし 1 ファイルも見ていない
$ npm run typecheck                    # ← tsc -b。こちらが本物
scripts/provenance.ts(31,19): error TS6133: 'relative' is declared but its value is never read.
scripts/searchTopology.ts(210,10): error TS2304: Cannot find name 'classifyFromEvaluation'.
```

`tsconfig.json` は `"files": []` と `references` だけを持つ**振り分け専用**です。
`-p` で直接指定すると「対象 0 件」で正常終了します。**`-b`（build mode）でないと
参照先（`tsconfig.app` / `node` / `scripts`）へ降りません。**

そのあいだ、テストもビルドも通っていました。**vitest も vite も型検査をしません。**
実際、未定義の関数を呼ぶコードが 1 コミット分すり抜けています
（`npm run search:topology` を実行して初めて `ReferenceError` で落ちました）。

**検査は必ず `package.json` の script 名で呼んでください。**
`npx` で直接叩くと、引数の違いで黙って対象が空になります。

| ✅ | ❌ |
|---|---|
| `npm run typecheck` | `npx tsc --noEmit -p tsconfig.json` |
| `npm run test` | — |
| `npm run build` | — |

### 検査器が「その規則を実装していない」こともある

**同じ日に 2 度目が出ました。**`schemas/` に `pattern` 制約を 5 本足したのに、
検証していた自前の validator が **`pattern` を実装していませんでした**。
schema に書いた時点で守られたつもりになり、report にもそう書きました。

意図的な違反 10 種で突き合わせると、**5 種が素通り**していました
（現物の artifact に違反は 0 件だったので、誤った値は公開されていません）。

いまは `ajv`（draft-07 の完全実装）に任せています → `npm run validate:profiles`。

**制約を足したら、その制約が実際に弾くことを 1 回試してください。**
「schema に書いた」と「schema が効いている」は別です。

### たまに落ちる検査は、落ちない検査より悪い

**2026-08-03、`npm run test` が 3 回に 1 回落ちる状態を自分で作りました。**

`test/trrs.test.ts` の「どの組み合わせでも、Tip 導体を含む橋絡は起きない」が、
**単独では 606 ms、全体では 5463 ms** かかっており、vitest の既定 timeout 5000 ms を
またいでいました。原因は同日に足した `test/sensitivityVariant.test.ts` が
`npx vite-node` の子プロセスを並列に起こし、他のワーカーを遅くすることです。

**このテストだけが、重いのに明示 timeout を持っていませんでした**（近隣は `30_000`）。

`vitest.config.ts` に `testTimeout: 30_000` を置いて解消しました。

> **ここの timeout は性能を測っていません。**ハングしたときに永久に止まらないための
> 保険です。負荷でばらつく値を合否に使うと、**「たまに落ちるけど気にしないテスト」**が
> 1 つ生まれます。それは空振りと同じ害です — どちらも検査を信用できなくします。

`vite.config.ts` ではなく **`vitest.config.ts`** が本物です
（間違えて `vite.config.ts` に書き、型検査を壊しました）。

### 書くときの規則

1. **成果物の有無で分岐しない。**`artifacts/` は git 管理下です。`existsSync` は使いません。
   読めなければ落ちるべきです。
2. **`?? []` / `?? null` で「引けなかった」を隠さない。**
   0 件と「引けなかった」が同じ値になる関数を書かない。`test/_must.ts` の
   `mustFind` を使えば、何を探して何があったかがメッセージに残ります。
3. **列挙した識別子は、実在を先に検査する。**
   ```ts
   for (const kind of KINDS)                      // ← まず実在を見る
     expect({ kind, inArtifact: a.major.some((e) => e.kind === kind) })
       .toEqual({ kind, inArtifact: true })
   for (const kind of KINDS) { /* ここで値を比べる */ }
   ```
4. **「n 件を検査した」という主張そのものを検査する。**
   `axesUsedPerVariant` が `{}` になると、「振った軸が効く」を 0 回検査したまま通ります。
   前提が空でないことを `mustBeNonEmpty` で見ます。
5. **`.find(...)!` を機械的に置き換えない。**41 か所ありますが、あれは落ちます。
   全部書き換えても守りは増えず、差分だけが増えます。

### 機械が確かめること — `npm run check:vacuity`

構文を洗い続けるのはやめました。**結果の側**を見ます。
空振りは形がばらばらでも、必ず次のどちらかで現れます。

- テストが **skip** される
- テストの **件数が減る**（ファイルが読めない／動的生成が 0 件になる／消された）

```
$ npm run check:vacuity
  実行: 220 件 / 8 ファイル (記録: 220 件)

空振りしているテストはありません。全件が実際に走っています。
```

`artifacts/test_counts.json` の `byFile` を**下限**として使います。
減らす変更が正しいこともあります（テストを統合した等）。そのときは
`npm run test:count` で更新し、**なぜ減ったかをコミットに書いてください。**
止めたいのは「黙って減ること」だけです。

> 検出器そのものも試しました。`describe.skip` を 1 つ入れると
> **飛ばされた 12 件を名指しで出す**ことを確認し、戻すと消えることも確認しています。

---

## 8. やってはいけないこと

- **artifact の数字を書き換えてテストを通す。**artifact はモデルの出力です。
  文書側を直すか、モデルを直してください。
- **「確認済み」「検証済み」と単独で書く。**何をどう検証したかを添えてください
  （静的検査／単体テスト／実ブラウザ／実機はすべて別のものです）。
- **解決した項目を「まだ仮定です」と言い続ける。**逆向きも嘘です（→ §1 (c)）。
- **合成量に、強いほうの入力の区分を付ける。**弱いほうに合わせてください（→ §1 (d)）。
- **語句をそのままテストで固定する。**モデルが進んだときに、テストが古い主張を守る側に回ります。
  実際に `halfPlugProfile.test.ts` が「接点位置を含めて全て仮定」を固定し続け、
  4極ジャックを実在部品ベースへ組み直したあとも公開 artifact に旧主張が残りました（→ §7・§9）。
  固定するのは語句ではなく**構造**（強い根拠と弱い根拠が分けて書かれていること）にしてください。
- **成果物の有無でテストを分岐させる。**`artifacts/` は git 管理下です（→ §7）。

---

## 9. 統合オーダーへの対応状況

Half-Plug Lab 側から 2026-08-03 付で監査オーダーを受けています
（監査対象 revision `ba58b4c`。当時の HEAD より 3 commit 手前）。
対応状況は [docs/INTEGRATION_ORDER_20260803.md](docs/INTEGRATION_ORDER_20260803.md) にあります。
