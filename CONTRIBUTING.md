# 変更するときに読むもの

> この文書の HTML 版（`docs/CONTRIBUTING.html`）は `npm run docs:html` で生成しています。**HTML を直接編集しないでください。**

このリポジトリは「根拠のはっきりしない数値を出さない」を方針にしています。
そのため**モデルを直すより、モデルを説明している場所を直すほうが手間がかかります。**

2026-08-02〜03 に不整合を 5 種類・**計 25 件**見つけました。
**5 種類とも根は 1 つで、「モデルを直したあと、それを説明している場所を全部は直せていない」**です。
以下はその記録と、いま何が機械で守られているかの一覧です。

---

## 1. 見つかった 5 種類

| 種類 | 件数 | 例 | いまの守り方 |
|---|---:|---|---|
| **(a) 文書の数値が artifact と食い違う** | — | 「±3 mm 動きます」（実際は非対称で最大 4.82 mm）／「37 件の仮定」（台帳のどの部分集合でもなかった） | **機械照合**（`test/docs.test.ts` の 68 件。artifact から期待値を組み立てる） |
| **(b) 画面の文字列が古い** | **5** | 4極ジャックの説明文が「Same Sky SJ3-35074A に準拠」のまま／ヘッダが構成モデルでも「実在する代表的な一例」と名乗る | **不変条件 6 件**（`test/uiStrings.test.ts`）。台帳方式は採らない → §2 |
| **(c) 逆向きの陳腐化** | **6** | 「Ring1/Ring2 の境界は仮定」（07-31 に FACT へ解決済み）／「4極ジャックのブレーク接点は定義していません」（08-02 に定義済み） | **個別テスト 5 件**。汎用の語句スキャンは採らない → §2 |
| **(d) 根拠区分が実態より強い** | **12** | 接点ばねの自由半径・ばね定数が DERIVED（実際は別メーカー資料への逆算）／`axisHeightFromPcb` が FACT（note 自身が「未確認」と書いていた） | **不変条件 3 件**。とくに「接点まわりで DERIVED を名乗れるのは図面の幾何から出るものだけ」は許される 3 件を名指しで固定 |
| **(e) 同一文書内で値が割れる** | **2** | README の不変量リストがすぐ下の注記と矛盾／REPORT が 8.06 と 8.05 を併記 | **個別テスト 2 件**。1 件しか出なかったので構造変更はしない → §2 |

> **(a) だけ件数を書いていないのは、機械照合を入れる前に見つけた分を数え直していないからです。**
> 現在は 68 件の照合が入っており、変異テストで全件が落ちることを確認しています。

---

## 2. 守り方を選んだ基準 — **先に数えてから決める**

守り方を増やす前に、**実際に何件あったかを数えます。**
このリポジトリでは、次の 3 回とも「数えたら、重い仕組みは要らなかった」になりました。

| 場面 | 母集団 | 実際のズレ | 採った守り方 |
|---|---:|---:|---|
| 画面の文字列 | 398 件（JSON 238 + UI 直書き 160） | **5** | 台帳ではなく**不変条件 6 件**。398 件の台帳は文言を直すたびに台帳も直すことになり、「台帳を通したから正しい」という誤った安心を生む |
| 根拠区分の note 矛盾 | 135 キー | **1**（語句スキャンは 9 件ヒット＝**誤検出 89 %**） | 語句スキャンは**採らない**。文脈（歴史的記述／同じ note 内の別の量）を見られないため恒久的なノイズになる |
| 文書どうしの食い違い | 6 主張 × 7 文書＝該当 257 行 | **1** | 「1 つの主張は 1 か所に書いて他は参照する」への**構造変更はしない**。方法の違いを書き添えるだけ |

**守らないという判断も記録します。**そうしないと、同じ検討を次の人がやり直すことになります。

---

## 3. モデルを変えたら、どこが連動するか

直近 8 コミットで `src/data/dimensions.json` と**同時に変わったファイル**の実測です。

| 連動先 | 誰が守っているか |
|---|---|
| `artifacts/verification_summary.json` の区分件数 → README / REPORT の根拠区分表 | **機械照合**（`docs.test.ts`） |
| 「ジャック内部の寸法は N 件の仮定を含み」（README 2 か所） | **機械照合**（`docs.test.ts`） |
| `artifacts/half_plug_topology_profile.v1.*.json`（区分は接点から伝播する） | **機械照合**（`halfPlugProfile.test.ts`） |
| 接点まわりの区分 | **不変条件**（`uiStrings.test.ts`） |
| 画面の variant 説明文・材質ラベル | **不変条件**（`uiStrings.test.ts`） |
| **テスト件数**（README / UNKNOWNS / `docs/TEST_RESULTS.md`） | **機械照合**（`artifacts/test_counts.json` と突き合わせ。→ §4） |

### 手順

1. `src/data/*.json` を直す
2. `npm run artifacts` — 走査結果と区分件数を作り直す
3. `npm run export:half-plug:all` — profile を作り直す（区分が変わったときは必須）
4. **`npm run check:stale`** — 重い成果物の再実行が要るかを**機械が判定します**（→ 下）
5. `npm run test` — **ここで連動漏れが落ちます**
6. `npm run test:count` — テスト件数の artifact を更新する
7. `npm run docs:html` — HTML を作り直す

### 重い成果物（10〜15 分）は、必要かどうかを機械に訊く

`npm run search:topology`（約 10 分）と `npm run sensitivity`（約 15 分）は毎回は回せません。
かといって回し忘れると**成果物だけが古い値のまま残ります。**

**条件を覚えないでください。`npm run check:stale` が判定します。**

```
$ npm run check:stale
  照合: topology_search_difference_signal.json: 軸 11 件

**再実行が必要です。**

  npm run search:topology -- --target DIFFERENCE_SIGNAL
    topology_search_difference_signal.json: jack.contact.sleeve.padWidth が 0.9 → 0.92 に変わっている
```

判定は**成果物自身の記録**で行います。`topology_search_*.json` は走査軸ごとに `shipped`（実行時の値）を、
`sensitivity.json` は `inputs`（走査に使った寸法値）を持っています。
現在のモデルと突き合わせるだけなので、**走査軸が増えても勝手に追随します。**
人がキーの一覧を保守しません。

> **2026-08-03 まで、ここには「接点位置や区分を変えたときは必要」と書いていました。
> これは条件として足りていませんでした。**下の通し確認で、
> 帰線パッド幅を変えたときも両方の再実行が要ることが分かったためです。

---

## 4. テスト件数だけは、ずっと手で直していました

2026-08-03 までの 8 コミットすべてで、テスト件数を **README / UNKNOWNS / `docs/TEST_RESULTS.md` の
3 か所**手で直していました。**24 回の手作業**です。

- **README と UNKNOWNS からは数を消しました。**読者にとって「207 件」と「212 件」の違いに
  意味はなく、手作業を増やすだけでした。
- **`docs/TEST_RESULTS.md` には残しました。**そこでは件数そのものが報告の内容だからです。
  代わりに `npm run test:count` が `artifacts/test_counts.json` を作り、
  文書の数字と機械照合します。

---

## 5. 手順を実際に通した記録

**「手順を書いた」と「手順が通ることを確かめた」は別です。**通した記録を残します。

### 2026-08-03 — 初回の通し確認

2 種類の変更で、上の手順だけを順に実行しました。

| | 変えたもの | 結果 |
|---|---|---|
| **A** | 表示専用の `note` を 1 行足す（`jack.nutDiameter`） | artifact 差分ゼロ・テスト 213 件すべて通過。**手順 2〜3 が空振りになるのが正しい挙動** |
| **B** | 帰線パッド幅 0.9 → 0.92（接触判定に効く） | 手順 2 で `events.json` / `contact_sweep.json` / `force_curve.json` が変化、手順 3 で profile 2 件が変化、**手順 5 のテストで 3 件が期待どおり落ちた**（README と REPORT の深さが `events.json` に無い、走査値と真値の取り違え） |

**手順は機能しました。**連動漏れは手順 5 で捕まります。

**ただし 1 つ穴がありました。**B で変えたのは「接点位置」でも「区分」でもなく**パッド幅**ですが、
`search:topology` も `sensitivity` も**パッド幅を走査軸に持っています**。
当時の条件（「接点位置や区分を変えたときは必要」）からはそう読み取れませんでした。
そこで `npm run check:stale` を作り、条件を機械判定に置き換えました。

> 検出器そのものも試しました。パッド幅を 0.92 にすると
> **キー名と必要なコマンドを名指しで出す**ことを確認し、戻すと消えることも確認しています。

## 6. やってはいけないこと

- **artifact の数字を書き換えてテストを通す。**artifact はモデルの出力です。
  文書側を直すか、モデルを直してください。
- **「確認済み」「検証済み」と単独で書く。**何をどう検証したかを添えてください
  （静的検査／単体テスト／実ブラウザ／実機はすべて別のものです）。
- **解決した項目を「まだ仮定です」と言い続ける。**逆向きも嘘です（→ §1 (c)）。
- **合成量に、強いほうの入力の区分を付ける。**弱いほうに合わせてください（→ §1 (d)）。
