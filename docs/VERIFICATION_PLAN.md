# 実物との突き合わせ計画

> この文書の HTML 版（同名 `.html`）は `npm run docs:html` で生成しています。**HTML を直接編集しないでください。**

作成 2026-07-31 ／ 更新 2026-08-02 ／ 状態: **実施予定なし（情報提供・実測の提供は歓迎）**

> ## この計画は「募集」であって「予定」ではありません（2026-08-02 方針）
>
> **メンテナ自身は実測を行いません。**電子計測の知見が無く、精度の要る測定を
> 自分でやると、かえって根拠の弱い数値を「実測値」として台帳に入れてしまうためです。
> このプロジェクトが避けたい失敗そのものです。
>
> | | |
> |---|---|
> | ❌ **やらないこと** | 部品の発注、自分での測定、実測値を待つこと |
> | ✅ **受け入れること** | 実測データの提供、図面や一次資料の情報提供、手順の誤りの指摘 |
>
> **モデルは「未実測のまま」で完結させます。**実測が来なくても、
> 何が仮定で何が図面由来かは全て記録済みで、感度解析で幅も測ってあります。
> 実測は精度を上げますが、**無いと成立しない設計にはしていません**。
>
> 以下の手順は、**測ってくださる方への手順書**として維持します。
> 手順・判定基準・期待値はすべて測る前に公開してあるので、
> **後出しで結論に合わせることはできません**（→ §5）。

> **2026-08-01 の変更**: 感度解析で、中心的な結論の土台（Tip と Ring の 0.15 mm の段差）が
> **両端とも幅を持つ量の差**だと分かりました（[UNKNOWNS.md](../UNKNOWNS.md) §4-1b）。
> これはノギス 1 本で決着するので、**§1-1 として独立させ、LED 回路を使う §1-2 より前に
> 置きました**。あわせて §1-2 にあった判定線 0.24 mm を実測で取り直しています（→ §1-1）。

このモデルの一番弱いところは、**ジャック内部の接点ばねの寸法をすべて仮定で置いている**ことです
（[UNKNOWNS.md](../UNKNOWNS.md) §3、[ASSUMPTIONS.md](../ASSUMPTIONS.md) A）。
データシートにもメーカー CAD にも書かれていないので、図面からは埋められません。

ただし幸運なことに、**その仮定は電気的に測れます。**
テスターで端子間の導通を見ながらプラグをゆっくり挿していけば、
接点が切り替わる深さが分かります。モデルはその深さを予測しているので、当たり外れが判定できます。

この文書は、その測定をどうやるかを決めたものです。

---

## 1. 何を測ると何が分かるか

内部で起きていること（接点の状態）はテスターでは見えません。見えるのは端子間の導通だけです。
そこで、モデルの出力を「テスターで見える形」に変換しました（`npm run predict:probe` で再生成できます）。

### 一番おいしい観測点 — ブレーク接点

**ジャックだけあれば測れて、しかも内部接点の位置がそのまま出ます。**
プラグに配線する必要すらありません。テスターの棒を2本のピンに当てるだけです。

#### Ring 側ブレーク接点（ピン2 ↔ ピン4）

| 深さ | すき間 | 変化 |
|---:|---:|---|
| 8.06 mm | 5.94 mm | 閉 → **開** |
| 9.43 mm | 4.57 mm | 開 → **閉** ← ここが山場 |
| 11.23 mm | 2.77 mm | 閉 → **開** |

**「いったん開いて、また閉じて、また開く」** という予測が出ています。
これは意図して作った挙動ではなく、プラグ先端の逆テーパ（先端の円錐の後ろで径が
φ3.02 → φ2.59 へ細くなる）を接点ばねが下ることで、押付が一時的にしきい値を割るためです。

（表の値は走査刻み 0.01mm での出力。刻みを 0.005mm にすると 8.05mm と出るなど、末尾 0.01mm は揺れる。
合否は ±0.3mm で見るので実用上は問題にならない。）

**この「再閉」が起きるかどうかが、この実験の核心です。**
起きれば、プラグ外形の実測と Ring 接点の位置が同時に裏付けられます。
起きなければ、どこかの仮定が間違っているとはっきり分かります。

#### Tip 側ブレーク接点（ピン3 ↔ ピン5）

| 深さ | すき間 | 変化 |
|---:|---:|---|
| 12.01 mm | 1.99 mm | 閉 → **開** |

こちらは1回だけ。素直な観測点です。

### 余力があれば — プラグ導体との導通

プラグの端子に線を出せば、こちらも測れます。半挿しの誤接触がそのまま見えます。

| ピン | 深さ | すき間 | つながる先 |
|---|---:|---:|---|
| ピン1（Sleeve 接点） | 3.93 mm | 10.07 mm | → **プラグ Tip**（帰線が左信号に触れる） |
| 〃 | 6.32 mm | 7.68 mm | → 切れる（絶縁帯を通過） |
| 〃 | 7.03 mm | 6.97 mm | → プラグ Tip |
| 〃 | 8.26 mm | 5.74 mm | → 切れる（第1絶縁帯を通過） |
| 〃 | 8.27 mm | 5.73 mm | → プラグ Ring |
| 〃 | 11.77 mm | 2.23 mm | → **Ring と Sleeve の両方**（橋絡） |
| 〃 | 11.95 mm | 2.05 mm | → **プラグ Sleeve**（ようやく正常） |
| ピン2（Ring 接点） | 8.01 mm | 5.99 mm | → プラグ Tip |
| 〃 | 10.04 mm | 3.96 mm | → 切れる |
| 〃 | 11.11 mm | 2.89 mm | → プラグ Tip |
| 〃 | 12.17 mm | 1.83 mm | → 切れる |
| 〃 | 12.34 mm | 1.66 mm | → **プラグ Ring**（正常） |
| ピン3（Tip 接点） | 11.96 mm | 2.04 mm | → **プラグ Tip**（正常） |

---

## 1-1. プラグ 1 個とノギスだけで決まること — **最初にこれをやってください**

**この計画で一番安く、一番早く、一番効くのがこれです。**
ジャックも LED もはんだ付けも要りません。**プラグ 1 個とノギスだけ**、実測 30 秒です。

### 何が決まるか

このモデルの中心的な結論は「第1絶縁帯（Tip↔Ring）では橋絡しない」ですが、
その根拠は **Tip 導体が Ring より 0.15 mm 低い（半径で）**という 1 点だけです。
そしてこの 0.15 mm は、**両端とも幅を持つ量の差**です。

| | 値 | 区分 | 幅 |
|---|---|---|---|
| Ring / Sleeve 半径 | 1.750 | **FACT** | 図面公差 φ3.5±0.05 → 半径 ±0.025 |
| Tip 後端半径 | 1.600 | **DERIVED** | 図面実測 φ3.20〜3.22 → 半径 +0.01 |

**実物がこの帯のどこにいるかは測っていません**（[UNKNOWNS.md](../UNKNOWNS.md) §4-1b）。
ノギスで直径を 2 箇所測れば、それが 1 つに決まります。

### 測り方

| 測る場所 | 予測 |
|---|---|
| Tip 導体の後端（先端から約 4.5 mm） | **φ3.20** |
| Ring 導体（先端から約 6.5 mm） | **φ3.50** |
| **その差** | **0.30 mm**（＝半径の段差 0.15 mm） |

Tip 後端は逆テーパの頂点なので、**先端から 4.3〜4.8 mm の間で最も太いところ**を探して読んでください。

### 読み方 — 線は 2 本あって、意味が違う

**この 2 本を混同しないでください。**（数値は `npm run sensitivity` の実測です）

| 実測の直径差 | 意味 |
|---|---|
| **0.23 mm 以上** | ✅ **図面の範囲内。** 図面から出る最悪の組み合わせ（外径 φ3.45 × 絶縁帯 φ3.22）がちょうど 0.23 mm です |
| 0.090〜0.23 mm | ⚠️ **図面より薄い。** 結論は保たれますが、実測の読み違いか、この個体が図面どおりでないかを疑ってください |
| **0.090 mm 未満** | ❌ **結論が崩れます。** ここまで薄いと、採用値 compliance 0.05 でも第1絶縁帯で Tip 橋絡が起きます |

**上の 2 つなら結論は保たれます。**ただし余裕の大きさは変わります。

| 実測の直径差 | Tip 橋絡が始まる compliance | 採用値 0.05 との比 |
|---|---|---|
| 0.30（公称） | 0.153752 | 3.08 倍 |
| 0.23（図面上の最悪） | 0.118875 | 2.38 倍 |
| 0.19 | ≈ 0.10 | 2.0 倍（**compliance の採用範囲の上端と等しくなる**） |
| **0.090** | **≈ 0.05** | **1.0 倍 = 破綻** |

> 2026-08-01 まで、ここには「**差が 0.24 mm を下回ったら実験の前提そのものが崩れます**」と
> 書いていました。**言い過ぎでした。**
> 実際に橋絡が始まるのは **0.090 mm** で、**2.7 倍離れています**。
> しかも 0.24 という線自体が中途半端で、図面が許す最悪値 0.23 のほうが下にありました
> （＝図面どおりの個体でも「前提が崩れた」と誤判定しかねない線でした）。

### 結果をどう反映するか

| 実測 | やること |
|---|---|
| 0.23 以上 | `plug.insulatorRadius` を実測値へ更新し、`tolerance` を実測のばらつきに置き換える。[UNKNOWNS.md](../UNKNOWNS.md) §4-1b を閉じる |
| 0.090〜0.23 | 同上。加えて余裕が縮むので、[SENSITIVITY.md](SENSITIVITY.md) §6-1 の箱を実測行に絞り、REPORT §2-3 の余裕の記述を直す |
| 0.090 未満 | **結論の撤回。** 第1絶縁帯で橋絡しないという主張を取り下げ、REPORT §4 と README を書き換える |

いずれの場合も、`npm run sensitivity` を流し直して数字を取り直してください（15 分）。

---

## 1-2. 決着実験 — 橋絡がどこで起きるか

**この 1 つの測定だけで、モデルの修正が正しかったかどうかが決まります。**

### 何が争点か

2026-07-31 の公開前監査で、接触判定に誤りが見つかりました。
接点が届いているかを「たわむ**前**の自由半径」で判定していて、
プラグに押し開かれたあとの位置で見直していなかったのです。

その結果、修正前と修正後で**予測が正面から食い違います**。

| | 第1絶縁帯（Tip↔Ring） | 第2絶縁帯（Ring↔Sleeve） |
|---|---|---|
| **修正前** | 深さ 8.27〜8.44 mm で**橋絡する** | 11.77〜11.94 mm で橋絡する |
| **修正後（現在）** | **一度も橋絡しない** | 11.77〜11.94 mm で橋絡する |

修正の根拠は、プラグ導体の**高さの差**です。Tip は最大 φ3.20（半径 1.600）、
Ring / Sleeve は φ3.50（半径 1.750）。**Tip だけ 0.15 mm 低い。**
接点は Ring に押し上げられた位置にいるので、0.15 mm 下の Tip 面には届かない ―― という理屈です。

### 先に §1-1 を済ませてください

**この実験の前に、§1-1（すぐ上）のノギス測定を必ず先にやってください。**

実物の段差が図面どおり（直径差 0.30 mm 前後）なら、下の導通試験で第1絶縁帯に
橋絡が出ないことが**予測どおり**の結果です。
しかし実物の段差が薄ければ、**橋絡が出るのが正しい**ことになります。
先に段差を知らずに導通試験だけをすると、**どちらの結果も解釈できません。**

ノギス測定はプラグ 1 個だけででき、この実験より先に終わります。

### 測り方 — プラグの Tip 線と Ring 線の間を見る

橋絡が起きると、ジャックの帰線接点が 2 つのプラグ導体を橋渡しします。
つまり**プラグ側の 2 本の線どうしが短絡します**。だからプラグ側だけを見れば分かります。

```
   LED + ボタン電池 + 抵抗（直列）
        ├── プラグ Tip 線
        └── プラグ Ring 線     ← この 2 本の間が短絡したら光る
```

**LED の直列回路を勧めます。** ただし理由は「速すぎるから」ではありません。
窓 0.18 mm を 0.2〜0.5 mm/s で通すと、短絡している時間は **0.36〜0.90 秒**あります。
導通ブザー（応答は数 ms）でも十分に捉えられる長さです。

LED を勧めるのは次の 2 点です。

- **接触が不安定なときにそれが見える。** ブザーは鳴る／鳴らないの二値ですが、
  LED はちらつきや薄明かりとして中間状態が出ます。判定表の「光らない」を
  「接触していない」と読んでよいかの確認になります。
- **ノギスの目盛りと同時に見られる。** 音だと、鳴った瞬間のすき間を読むのに
  目を離す必要があります。

1. プラグに Tip / Ring / Sleeve の 3 本の線を出す
2. LED 回路を Tip 線と Ring 線につなぐ
3. すき間 6.0 mm から 5.3 mm までを **0.2〜0.5 mm/s** でゆっくり通す
4. **一瞬でも光るか**を見る。**3 回以上繰り返す**

### 判定は 2×2 で決まる

対照実験を同時にやります。**プラグの Ring 線と Sleeve 線**の間も同じ回路で見てください
（すき間 2.5 mm → 1.8 mm を同じ速度で通す）。こちらは**修正前も修正後も光る**予測です。

| Ring↔Sleeve（対照） | Tip↔Ring（本命） | 判定 |
|---|---|---|
| **光る** | **光らない** | **修正後のモデルが正しい。** 高さの段差の解釈が裏付けられた |
| **光る** | **光る** | **修正前が正しく、修正が誤り。** 接点は 0.154 mm 以上下の面まで追従している |
| 光らない | — | **測定が成立していない。** 配線・速度・LED 回路を見直す。またはパッド幅が 0.72 mm 以下（本命の陰性は無意味） |
| 光らない | 光る | ありえない。配線ミスを疑う |

対照が光ることが、**「0.18 mm の一瞬の短絡を検出できる装置になっている」ことの証明**です。
これが無いと「Tip↔Ring が光らなかった」に意味がありません。

### なぜこの測り方が強いか

**深さを測る必要がありません。** 見るのは光るか光らないかだけです。

深さで判別しようとすると失敗します。修正前後で予測が食い違う深さの最大差は **0.180 mm** しかなく、
合否基準の ±0.3 mm を下回るからです。**この論点は深さの測定では決着しません。**

もうひとつ、この実験には非対称性があります。
**修正後のモデルは、パッド幅をどれだけ広げても Tip↔Ring 短絡を起こせません。**
だから一度でも光れば、パラメータ調整では逃げられず、修正が無条件に反証されます。

### おまけ — 対照の窓幅からパッド幅が逆算できる

Ring↔Sleeve が光っている間のすき間の幅を測ると、帰線接点のパッド幅が出ます。

```
パッド幅 ≒ 光っている窓の幅 + 0.72
```

いま 0.9 mm と仮定している `jack.contact.sleeve.padWidth` を、ASSUMPTION から上げられます
（予測: 窓幅 0.18 mm）。これは余談ではなく、**本命の陰性結果を解釈するために要ります。**
窓が見えなければパッド幅は 0.72 mm 以下で、そもそも Tip↔Ring 試験が成立しません。

### ブレーク接点では決着しません

§1 の「一番おいしい観測点」（ピン2↔ピン4、ピン3↔ピン5）は、この論点に対して**判別力がゼロ**です。
開閉する深さは修正前後でまったく同じ（8.06 / 9.43 / 11.23 / 12.01 mm）でした。

あちらはプラグ外形と接点の軸位置を試す実験で、橋絡の真偽は試せません。
**この決着実験にはプラグ側への配線が必須**です。プラグ 1532 10 ははんだ付け端子を持つので、
3 本のリード線をはんだ付けするか、市販の 3.5 mm ステレオケーブルを切って各線を確認してください。

---

## 2. 深さをどう決めるか

**深さそのものを測るのは難しいので、「すき間」を測ります。**

```
   プラグ肩            ジャック前面
      │                    │
      │◀──  すき間  ──▶│
      └────────────────────┴──── 奥
                              深さ = 14.00 − すき間
```

完全に挿し込むとプラグの肩がジャック前面に当たって止まります。この位置が深さ 14.00 mm です
（プラグ肩の外径 φ10（図面記載）がボア φ3.6 より大きいため物理的にそこで止まる）。
つまり**すき間ゼロ＝深さ 14.00 mm** で、自動的に基準が決まります。

すき間は 1.6〜10 mm の範囲なので、ノギスの外側ジョーやデプスバーで素直に測れます。

### 測り方（1チャンネルずつ）

目標の深さに合わせにいくのではなく、**変化した所で止めて測る**ほうが簡単で正確です。

1. ジャックを動かないように固定する（万力・両面テープ・ブレッドボードなど）
2. テスターを導通ブザーにして、ピン2とピン4に当てる（クリップ付きが楽）
3. プラグをごくゆっくり押し込む
4. ブザーが切れた（または鳴った）瞬間に手を止める
5. その状態でプラグ肩とジャック前面のすき間をノギスで測る
6. 深さ = 14.00 − すき間 を記録する
7. 一度完全に抜いて、**3回繰り返す**（ばらつきを見るため）
8. ピン3とピン5でも同じことをする

プラグは指で押すと傾きます。まっすぐ入るよう、指先で軸を支えてください。

---

## 3. 必要なもの

### 本体（欧州から取り寄せ）

| 品目 | 型番 | 数 | 在庫（2026-08-01 メーカーサイト調べ） |
|---|---|---:|---|
| ジャック | Lumberg **1503 09** | 2 | Farnell GB 1,873 ／ Arrow DE 1,000 ／ Bürklin DE 529 ／ TME PL 431 |
| プラグ | Lumberg **1532 10** | 1 | Lumberg DE 1,200 ／ Bürklin DE 501 ／ Arrow DE 38 |

> **プラグの方が探しにくい。** 1532 10 は TME / Farnell / RS の検索に出てきません
> （同じ 1532 系でも 02 / 04 / 05 / 06 しか並びません）。
> メーカー自身のページには在庫が出ているので、**Bürklin か Arrow、あるいは
> Lumberg への直接問い合わせ**が確実です。

### 測定4（決着実験）の LED 回路

| 品目 | 目安 | 備考 |
|---|---|---|
| 赤色 LED | Vf 約 1.8〜2.1 V | 秋月電子などで数十円。低電流で光るものが見やすい |
| 抵抗 | 100〜330 Ω | CR2032 3 V − Vf 2 V で 3〜10 mA 程度になる値 |
| CR2032 + ホルダー | 電池 ¥200/5個・ホルダー ¥75〜100 | 秋月 通販コード 105694（電池）/ 101443・100706（ホルダー） |
| 配線 | みの虫クリップ付きリード | プラグの Tip / Ring / Sleeve から線を出すため |

> CR2032 は内部抵抗が大きい電池ですが、**この用途では問題になりません**。
> 見るのは 0.36〜0.90 秒の点灯で、必要な電流も数 mA だからです。
> 手持ちの単3×2 本（3 V）でも同じことができます。

### 測定器

| 品目 | 目安 | 概算 | 備考 |
|---|---|---|---|
| デジタルノギス | 0.01 mm 表示 | ¥2,000〜4,000 | **§1-1 はこれとプラグ 1 個だけで完結**。§2 のすき間測定にも必須 |
| テスター | 導通ブザー付き | ¥1,500〜3,000 | §1・§2 用。クリップ付きリードがあると格段に楽 |

> **§1-1 だけなら、ジャックも LED 回路もテスターも要りません。**
> プラグ 1 個（€2〜4）とノギスがあれば、届いたその日に 30 秒で終わります。
> しかも [UNKNOWNS.md](../UNKNOWNS.md) §4-1b がそれで閉じます。
> **費用対効果はこの計画の中で断トツです。**

### 費用と納期

| | 概算 |
|---|---|
| 部品（ジャック2・プラグ1） | €6〜10 |
| 送料（欧州発） | €15〜25 |
| LED 回路一式 | ¥500 以下 |
| ノギス・テスター（未所持なら） | ¥3,500〜7,000 |

**部品と送料だけなら ¥4,000〜6,000、納期 2〜4 週間。**
一番高いのは送料なので、他に欲しい部品があればまとめて頼むほうが得です。

### 買える所

**両方を 1 か所で買えるのは Bürklin と Arrow です。**（ジャックだけなら選択肢はもっと広い）

| 販路 | ジャック 1503 09 | プラグ 1532 10 |
|---|---|---|
| [Bürklin](https://www.buerklin.com/en/p/lumberg/phono-connectors/1503-09/41P1263/)（独） | ○ 529 | ○ 501 |
| Arrow（独） | ○ 1,000 | ○ 38 |
| Lumberg 直（独） | — | ○ 1,200 |
| [Farnell](https://uk.farnell.com/lumberg/1503-09/connector-rca-jack-3-5mm-3way/dp/1243244)（英） | ○ 1,873 | ✗ 掲載なし |
| [TME](https://www.tme.com/us/en-us/details/1503-09/jack-connectors/lumberg/1503-09/)（波） | ○ 431 | ✗ 掲載なし |
| [Rapid](https://www.rapidonline.com/lumberg-1503-09-jack-socket-3-5mm-angled-chassis-pcb-mount-3-pole-stereo-54-4736)（英） | ○ | ✗ 掲載なし |

在庫数は 2026-08-01 に[メーカー製品ページ](https://www.lumberg.com/en/products/product/1532+10)で確認した値です。
横断比較には [Octopart](https://octopart.com/1503+09-lumberg-5404534) も使えます。

> **日本向けの送料と最低注文額は未確認です。** Bürklin / Farnell の該当ページが
> 自動取得を拒否したため、金額を裏取りできていません。**発注前にご自身で確認してください。**
> Farnell は日本法人（jp.farnell.com）があるので、ジャックだけならそちらが早い可能性があります。

> データシートの PU（包装単位）は 500 個ですが、これはメーカー出荷の単位です。
> 上記の流通各社はバラ売りしています。

### 予備を買うか

ジャックは**2個**買うことを勧めます。1個は測定用、1個は必要になったとき切断して
内部を直接見るためです。単価が安いので、選択肢を残しておく価値があります。

---

## 4. 合否の基準

「だいたい合っていた」で終わらせないために、先に線を引いておきます。

| 予測とのずれ | 判定 | やること |
|---|---|---|
| **0.3 mm 以内** | モデルは実測と整合 | `dimensions.json` の note に実測日と実測値を追記。grade は DERIVED のまま（実測で裏付いたと明記） |
| **0.3〜1.0 mm** | 位置がずれている | 逆算した軸位置へ更新し、grade を ASSUMPTION → DERIVED に上げる |
| **1.0 mm 超**、または再閉が起きない | 構造の仮定が違う | 軸位置だけの問題ではない。パッド幅・自由半径・プラグ外形のどれが効いているか切り分ける |

0.3 mm という線は、**測定側の誤差の見積り**から決めています。
ノギスの読み ±0.02 mm、切り替わり位置の判断 ±0.1 mm、プラグの傾きによる影響 ±0.1 mm 程度を
足し合わせると 0.2 mm 強になるので、その外側に置きました。

### 定性的な合否（数字より重い）

- **Ring ブレーク接点の「再閉」が起きるか** — 起きなければ、プラグ先端の逆テーパの解釈か
  Ring 接点の自由半径が違います。数値のずれより重大です
- **開閉の回数** — 予測は「開・閉・開」の3回。回数が違えば構造の理解が違います
- **ピン1が最初に触れる相手がプラグ Tip か** — 帰線用の接点が左信号に触れるという予測。
  ここが違うと接点の並び順の理解が違います
- **第1絶縁帯で Tip↔Ring が短絡しないか** — §1-2 の決着実験。鳴ってしまったら、
  接触判定の修正そのものが間違っていたことになります。**深さの一致より重い判定です**

---

## 5. 測った後どうするか

逆算の道具を用意してあります。**測った深さを渡すと、その深さになる接点位置を計算します。**

```bash
npm run fit:contacts -- 8.4 12.3 4.1
```

引数の順は「Ring ブレーク開放 / Tip ブレーク開放 / Sleeve 接点が Tip に触れ始める」の深さ（mm）です。

出力されるもの:

- 逆算した軸位置と、現モデルからのずれ
- **合わせていない残りの切り替わり**が、その軸位置ならどうなるか

最後のが大事です。合わせるのは各接点につき1点だけにしてあります。
全部を合わせにいくと「合って当然」になり、検証したことになりません。
1点で合わせて、残りが当たるかどうかを見る。それがモデルを試すということです。

引数なしで実行すると自己検査になります（モデル自身の予測を入れて元の軸位置を復元できるか）。
**現状の復元誤差は 0.0000 mm** です。道具そのものは正しく動いています。

```bash
npm run fit:contacts
```

### 反映の手順

1. 実測値を `docs/measurements/2026-XX-XX.md` として記録（生データ・3回分・気温も）
2. `npm run fit:contacts -- ...` で逆算
3. 判定表に従って `src/data/dimensions.json` を更新
4. `npm run test` と `npm run artifacts` を回し、予測がどう変わったか差分で確認
5. [UNKNOWNS.md](../UNKNOWNS.md) §3 の該当項目を「解決済み」に移し、実測日と方法を書く
6. [TEST_RESULTS.md](TEST_RESULTS.md) に実測の節を足す

**§1-1（ノギス）だけは経路が違います。** 逆算は要りません。

1. `plug.bodyRadius` と `plug.insulatorRadius` の `value` を実測値へ、`tolerance` を
   実測のばらつき（3 回測った幅）へ置き換える
2. **`npm run sensitivity` を流し直す**（15 分）。この 2 つの `tolerance` が振り幅の正本なので、
   公差の箱と最悪コーナーが自動で実測ベースに変わります
3. [SENSITIVITY.md](SENSITIVITY.md) §6-1、[UNKNOWNS.md](../UNKNOWNS.md) §4-1b、
   [ASSUMPTIONS.md](../ASSUMPTIONS.md) A-2、[REPORT.md](REPORT.md) §2-3 の数字を差し替える
4. `plug.insulatorRadius` の grade を **DERIVED → FACT**（実測）へ上げ、
   [SOURCES.md](../SOURCES.md) §5 に実測日と方法を追記する

---

## 6. この実験で分からないこと

正直に書いておきます。

- **接点ばねの形そのもの**（板厚・梁の長さ・曲げ方）は分かりません。分かるのは
  「どの深さで切り替わるか」から逆算した**接触点の軸位置**だけです。
  形を知るには切断するしかありません（だから予備を買う）
- **押付力（N）** は測れません。ばね定数は依然として仮定のままです
- **接触抵抗の絶対値** は、この方法では意味のある精度で出ません。
  導通ブザーが鳴るかどうかしか見ていないためです
- **4極（TRRS）側の仮定** は、この実験では一切検証されません。別部品が必要です
- **接触ドームの追従量そのもの**（0.05 mm と仮定）は測れません。決着実験で分かるのは
  「0.118 mm より小さいかどうか」だけで、その中のどこかは決まりません

つまりこの実験で ASSUMPTION から降ろせるのは「接点の軸位置3つ」と、
間接的に「プラグ外形の解釈」です。37件の仮定のうち片付くのは数件で、
残りは残ります。それでも、**一番効く数件**です。

---

## 7. 進め方

**メンテナはこの手順を実施しません**（冒頭の方針）。以下は測ってくださる方への順序です。

| 段階 | 内容 | 誰が |
|---|---|---|
| 1 | 部品を用意する（プラグ 1 個だけでも §1-1 は成立する） | 測る人 |
| 2 | **§1-1 のノギス測定（プラグ 1 個・30 秒）** | 測る人 |
| 3 | 余力があれば §1-2 の導通試験、§2 の深さ測定 | 測る人 |
| 4 | 結果を Issue か PR で共有（生の読み・3 回分・使った器具） | 測る人 |
| 5 | 台帳への反映、感度解析の再実行、文書の更新 | メンテナ |

**段階 2 を段階 3 より先に置いているのは意図的です。** §1-1 はプラグ 1 個とノギスだけででき、
はんだ付けも LED 回路も要りません。しかも §1-2 の導通試験の結果を解釈するには、
先に実物の段差を知っている必要があります（§1-2 冒頭）。
**一番安い測定が、一番効きます。§1-1 だけでも十分に価値があります。**

### 提供していただく場合

- **生の読みをそのまま**ください。平均や補正はこちらで行います（`docs/measurements/TEMPLATE.md`）。
- **「合わなかった」結果も同じくらい有用です。** このモデルは反証されうる形で
  数値を公開してあります。合わない報告は、モデルが間違っている証拠として扱います。
- 器具の分解能と、測った個体のロットが分かれば添えてください。
- **§1-1 だけ、1 回だけでも構いません。**

部品の到着を待つ間にできることは、このモデルの外側にあります（ライセンス整備・公開前監査など）。
測定そのものは部品が届くまで始められません。
