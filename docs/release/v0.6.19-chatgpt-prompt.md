# v0.6.19 — 監査スレッドへ渡す作業指示

添付は公開物から取得したものです。**まず `shasum -c SHA256SUMS` を通してから読んでください。**
以下の数値は、すべて添付のファイルから機械照合できます。報告文からの転記ではありません。

```
添付 30 件 / shasum -a 256 -c SHA256SUMS   exit 0 / 29 OK / 0 FAILED
  対照  1 バイト足すと exit 1、戻すと exit 0
SHA256SUMS  ae0ae0b34802fe07774537c676916005e72069e02dc898cd2996f37877195142
```

外部監査 P2（core / CLI 分離）は、この版で完了しました。

---

## 1. **`toolVersion` も `schemaVersion` も上げていません**

`main(args, io)` を抽出しましたが、**受け手の分岐は 1 つも変わりません。**

```
toolVersion    20 のまま
schemaVersion  2 のまま（…cli-result.v2）
catalog        82 種類のまま（増減 0）
```

**根拠（すべて実測）:**

```
出力          分離前に固定した **10 経路すべてが byte 一致**
              stdout / stderr / 終了コードの sha256 で比べる
              基準は dab485c2260c 時点・toolSha256 b208399106bb1d97…（分離前の道具）
出しうる code  82 種類のまま
判定ロジック   **差分 0 行**（触ったのは出口の形と設定の作り方だけ）
```

基準は repo に入っています（`test/fixtures/cli-output-baseline.v1.json`）。
`npm run check:cli-output` で**そちらでも同じものを回せます。**CI にも段として入っています。

**`JSON.parse` して比べていません。byte で比べます**
——キーの順序が変われば受け手の差分も変わるためです。

## 2. `done()` は「返す」形にできませんでした。**これは計画の 2 択の枠内です**

計画書（`docs/CORE_CLI_SEPARATION_PLAN_20260815.md`）§3 はこう書いていました。

```
75:  `done()` は `process.exit` を呼ばず、結果を返す形にします。
77:  **そこを「投げる」か「返す」へ変える**のが実質の作業です。
```

**2 択のうち「返す」が選べませんでした。**実測:

```
done() の呼び出し 6 か所のうち、**5 か所は直後にコードが続く**
  2995 行 → 直後 2997   3006 → 3011   3015 → 3021   3048 → 3067   3217 → 3226
  3300 行だけが本体の末尾
```

返す形にすると、**結果を出したあとも処理が進みます。**
`CliResult` を投げ、`main()` が受けて CLI 側へ渡す形にしました。

**例外ですが異常ではありません。**`done()` が呼ばれた時点で結果は確定していて、
あとは呼び出し元まで戻るだけです。`instanceof` で見分けるので途中の `catch` には拾われません。

## 3. **`io` の注入だけで `SOURCE_ARCHIVE_MISSING` を踏めるようになりました**

v0.6.18 の notes で、こう書きました。

> `io` へ filesystem を足しましたが、**この経路には効いていません。**
> 残した理由は先の話です——`main(args, io)` を抽出したあとは、
> global を差し替えずに fs を注入できるようになります。

**その土台が、この版で効きます。**

```
global を 1 つも差し替えず、io だけを渡して main() を呼ぶ
  → SOURCE_ARCHIVE_MISSING / exit 2
  対象 path の existsSync が呼ばれた回数 = 2（1 回目は通し、2 回目で false）
対照  差し替えなければ、同じ引数で別の code になる
```

`main()` が `io` しか見ていないことも確かめています。

```
cwd を差し替える     → manifest を見失う（MANIFEST_MISSING）
引数を空で渡す       → CLI_ARGUMENTS_MISSING（process.argv を見ていない）
```

**「効いていない」と書いたものが、あとで効いた**という形です。
効いていない時点で「効いている」と書かなかったので、この確認ができました。

## 4. 子プロセスの route 表と `io` 注入の、**両方を残しました**

`io` 注入ができたので表を片付けたくなりますが、**寄せられません。**

```
子プロセスの表（test/_cliRoutes.mjs）
  配った 1 ファイルを、受け手と同じ起動のされ方で踏む
  → 終了コード・stdout の byte・入口の判定まで含めて確かめられる

io 注入（test/mainInjection.test.ts）
  本体が io しか見ていないことを示す
  → global を差し替えないので「差し替えが効いていただけ」を排除できる
```

**`io` 注入は子プロセスへ効きません。**あの表は `spawnSync` で起動するので、
呼び出し側のオブジェクトは相手に届きません。
**同じ code を、別の入口から踏みます。**どちらか一方では足りません。

## 5. **「計画と違った」と書きかけて、自分の計画の次の行を読み落としていました**

notes と実施記録に、最初こう書きました。

> 計画では「`done()` は `process.exit` を呼ばず、結果を返す」と書いていました。

**計画書の次の行に「投げるか返すか」と書いてありました**（§2 に引用したとおり）。
1 行だけを見て「計画と違った」と報告するところでした。
実際は **2 択のうち「返す」が選べなかっただけ**で、計画の枠内です。

**逸脱の報告は信用に直結します。**「計画と違った」は
「計画が甘かった」という含みを持つので、事実でないなら書いてはいけません。
逆に本当の逸脱を「枠内」と書くのも同じだけ悪いことです。

両方の文書を実物に合わせ、**読み違えた経緯も実施記録へ残しました**
（直した跡が消えると、学びも消えるので）。

> 引用するときは**その段落を最後まで**読む、という当たり前を落としました。
> 1 文だけ抜くと、条件や選択肢を書いた続きの文が落ちます。
>
> **この文書でも同じことをやりました。**上の引用に最初 `74:` / `76:` と書きましたが、
> 実物は `75:` / `77:` です（実施記録を追記したぶん行がずれていました）。
> 「行番号つきで実物から取る」と書いた同じ節で、**記憶の行番号を書いていました。**
> 機械照合が捕まえたので直しています。

## 6. 途中で 2 回止まりました

### ① 非 async のまま包んで構文エラー

`main()` を非 async の関数で包んだので、本体の `await` が構文エラーになりました。

```
SyntaxError: Unexpected reserved word
  const loaded = await (SOURCE_DIR
```

基準との突合が落ちたので、そこで止めて中身を見ました。

### ② `tail` の切り取りで、規模を読み違えかけました

そのとき突合の出力を `| tail -3` で見ました。

```
tail -3 で見えたもの   最後の 1 経路の 2 欄だけ
実際                   **全 10 経路 × 4 欄 = 40 件**が相違
```

「1 経路だけの問題」と読んで、**その経路の注入が悪いという存在しない仮説**を
追うところでした。総数を数え直して全件と分かり、原因（`async` 忘れ）へ届きました。

**出力を絞るのは読みやすさのためですが、絞った結果が「規模」の判断材料になります。**
規模を読み違えると、原因の当たりも外れます。

> もう 1 つ小さいものがあります。「CLI 側で `done` を呼んでいない」を
> 素の文字列で探す検査を書いて落ちましたが、当たったのは**説明文の中の `done()`** でした。
> コメントを除いてから見る形にし、**除いた結果が空でないこと**も確かめています。

## 7. **公開物で attestation の 3 digest を検算しました**

v0.6.17 の欠陥（`releaseIndexSha256` が repo 側の索引を指していた）を v0.6.18 で直しました。
**この版でも成立していることを、公開物そのもので確かめています。**

```
v0.6.19 の添付で attestation の digest を突き合わせる
  testCountsSha256         2ee1d95060214b20…  一致
  validationResultsSha256  40cc19f7f02f3f96…  一致
  releaseIndexSha256       5570579b54df337c…  一致
  → 3/3
```

**この検算を毎回やるのは、v0.6.17 でここだけがずれていたからです。**
「直したから大丈夫」ではなく、**版ごとに公開物で測ります。**

## 8. 次は `io` に fetch を足します

```
やる     io へ fetch を足し、SOURCE_FETCH_FAILED / _TIMEOUT / SOURCE_HTTP_ERROR /
         SOURCE_BODY_UNREADABLE を **global を差し替えずに**踏めるようにする
検証     同じ基準（10 経路）で byte 一致を確かめる。1 バイトでも動いたら止める
やらない 子プロセスの route 表を消すこと（§4 のとおり両方要る）
```

**接点の追加であって抽出ではない**ので、この版とは分けました。

## 9. 反例のもらい方について（お願い）

**指示書が機械で実行できる形**だと速く確かめられます。

```
schemas/… を <tag> から読む
{"status":"…","stableReasonCode":"…"} を AJV で当てる
diffSchemaObjects(A, B) が BUMP になることを確認する
```

「〜のはずである」ではなく、
**「この 2 つのファイルへこのコマンドを当てるとこうなる」**が最も速く確かめられます。

今回 §5 と §6 は指摘ではなく自分の誤りですが、
**基準（10 経路の byte 比較）があったから止まれました。**
機械で確かめられる形にしておくと、自分の誤りも同じ仕組みで捕まります。

---

## 数えたもの（すべて添付から機械照合できます）

```
tag            v0.6.19（annotated・署名なし）→ commit dea7d7a77d3801b652419dabf675ef4942202702
release        29 本目 / latest = v0.6.19 / asset 30 件
検算ツール      toolVersion 20 / sha256 b8d555bbc9048f7f783d243bf26201c82e21ce0df6b52847da0f19de4e516144
CLI 結果契約    schemaVersion 2（…cli-result.v2）
attestation    stageToolVersion 1 / READY / exact true / cross true
索引           releaseTag v0.6.19 / releaseCommit dea7d7a77d38 / asset 27 件
test_counts    1371 件 / 37 ファイル / skip 0 / failed 0
validation     schemaVersion 3 / 14/14 適合 / READY
catalog        82 種類 = corpus 45 / cli-route 34 / defensive-invariant 3
出力の基準      10 経路 / takenFromCommit dab485c2260c / toolSha256 b208399106bb1d97…
profileId      TRS  trs-jack-3d:TRS|JACK-TRS:cdcdc82bd079
               TRRS trs-jack-3d:TRS|JACK-TRRS:83f95581fd2d
```

**`profileId` はこの文書から転記せず、同梱の `trs-jack-3d-release-index.v1.json` から引いてください。**

## 変わっていないもの

```
verifiedPhysical        false
measurement records     0
導通・音響               未実施
IV028                    13.30–13.52 mm / ASSUMPTION
TRS   区間 23 / event 32
TRRS  区間 30 / event 36
```

**モデルの数値は 1 つも動いていません。**葉ノードで比較して実測しています
（第1段: 由来 4 件・モデル 0 件 ／ 第2段: 由来 8 件・モデル 0 件。
対照として 1 つの値を 0.001 動かすと「モデル数値の差 1 件」が出ます）。

過去 release の asset は **v0.1.0〜v0.6.18 の 723 件すべて byte 一致**を、
再生成の段ごとに対照つきで実測しています（対照: 基準を 1 行壊すと 722 件へ落ちる）。
