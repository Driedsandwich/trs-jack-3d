# v0.6.4 — **schema は 1 本も上げていません。下流はどこでも止まりません**

## ⚠ 先に読むところ

### 1. **検算ツールがまた `OK` と言いながら別のものを渡せました**（`toolVersion` 9）

v0.6.3 で直したつもりの穴が、**3 つ残っていました。**
外部監査（2026-08-06）が反例を出し、こちらで再現してから直しています。

GNU tar が手元に無いので、**bsdtar 3.5.3（libarchive 3.7.4）と Python 3.14 の `tarfile`**
の 2 実装を oracle にしました。監査は GNU tar 1.35 と比べています。

> **どれが「正しい」かを決める必要はありません。実装間で結末が割れること自体が欠陥です。**

**同じ member に名前の上書きが 2 つ効くと、結末が割れていた**

v0.6.3 は PAX の `path=` を解釈するようにしましたが、GNU long name（`L`）と
**同じ変数へ後勝ちで**置いていました。

```
                          検算 v8     bsdtar      python
PAX path= → GNU L         gnu.txt     pax.txt     pax.txt     ← 両方と食い違う
GNU L → PAX path=         pax.txt     gnu.txt     gnu.txt     ← 両方と食い違う
PAX path= を 2 回          two.txt     拒否        one.txt     ← 三者が全部違う
PAX path= → PAX x(mtime)  pax.txt     拒否        pax.txt
```

v9 は上書き機構が 2 つ効いたら止めます。global header（`g`）が `path` を持つ場合も止めます。

**展開されるのに、検算の母集団から消える entry があった**

v0.6.3 は通常ファイル（`typeflag 0`）だけを内部の Map に入れ、
**未記録入力の探索もその key しか見ていませんでした。**

配布中の v0.6.3 の道具で直接確かめた実測です。

```
scope 配下（src/model/）に symlink を仕込んだ source archive を渡す
  v0.6.3  status OK        未記録候補 []                      exit 0
  v0.6.4  status MISMATCH  未記録候補 ["src/model/sneaky.ts"]  exit 1
  対照     正常な source     未記録候補 0 件                     exit 0
```

`typeflag 7`（contiguous）も同じ形です——検算からは見えず、bsdtar と python は
どちらも**通常ファイルとして展開**します。

v9 は**全 entry の型つき一覧**を作り、範囲の完全性検査はそちらを母集団にします。
**リンクは止めません。**正当な archive にも出てくるので、止める代わりに見えるようにしました。

**パスの不正なバイトが U+FFFD へ置換されていた**

```
raw   root/file<FF>.txt
  検算 v0.6.3  root/file<FFFD>.txt を検証して status OK
  bsdtar       生バイトのまま扱う（別のファイル名）
  python       生バイトのまま扱う（別のファイル名）
```

**置換して続けると、検算が見た名前と展開してできる名前が別物になります。**
v9 は `TextDecoder('utf-8', { fatal: true })` で読み、読めなければ止めます。

### 2. **差分試験そのものが片方向でした**

v0.6.3 で入れた「実展開との差分試験」は、**検算器が返した key を展開木で探す**方式でした。
これは **「展開されるのに検算器が数えない」欠陥を見られません。**

実測: `typeflag 7` の検査を外す変異を入れても、片方向の 48 件は **1 件も落ちませんでした。**

v0.6.4 では逆向きも見ます。

```
展開してできた通常ファイルは、検算器の files か inventory のどちらかに
現れていなければならない。どちらにも無いなら、
**検算器から見えていないファイルが source に混じる。**
```

どの検査が効いているかは、1 つずつ外して測りました。

| 外した検査 | `tarHardening` | 実展開 oracle |
|---|---:|---:|
| 名前の上書きが 2 つ効く形を止める | 3 | 2 |
| 中身を持つ未対応 type を止める | 2 | 0 |
| パスの厳密 decode | 3 | 3 |
| inventory に全 entry を載せる | 0 | 2 |

**どちらか一方の試験だけでは足りません。**上 2 つと下 2 つで、捕まえる側が入れ替わります。

### 3. `profileId` が変わりました。**lock を作り直してください**

```
v0.6.3  trs-jack-3d:TRS|JACK-TRS:a82235bb7297
v0.6.4  trs-jack-3d:TRS|JACK-TRS:104e6a87aa90

v0.6.3  trs-jack-3d:TRS|JACK-TRRS:9f8d61a4090b
v0.6.4  trs-jack-3d:TRS|JACK-TRRS:d8d7a2930de1
```

**この文書から転記せず、同梱の `trs-jack-3d-release-index.v1.json` から引いてください。**

**動いた原因は版数上げ（`package-lock.json`）です。**検算ツールは配布物ですが
**入力ではない**ので、そちらは digest を動かしません。

**モデルの数値は変わっていません。**葉ノードまで平坦化して v0.6.3 と比べた実測です。

```
TRS   葉 2267 件 → 由来まわりの差 8 件 / モデル数値の差 0 件
TRRS  葉 3130 件 → 由来まわりの差 8 件 / モデル数値の差 0 件
```

この比較が「全部を由来に寄せているだけ」ではないことも確かめてあります——
v0.6.3 の profile の `fullInsertionDepthMm` を 14 → 14.001 に変えて同じ比較をすると、
**「モデル数値の差 1 件」**になります。

### 4. **契約は変わっていません**

```
v0.6.3 の schema 22 本 → v0.6.4
  HOLD 22 / HOLD_RECORD 0 / **BUMP 0**
新設 0 本 ／ schemas/ の byte 差分 0 行
```

---

## 塞ぎすぎていないこと

**一度、塞ぎすぎました。**PAX の**値**を一律に厳密 decode したところ、
実物の macOS `tar` が作った tarball を弾きました（`LIBARCHIVE.xattr.*` /
`SCHILY.xattr.*` に**生バイナリ**が入ります）。
**v8 でも同じ形の失敗をしており、2 回目です。**
解釈する値（`path`）だけ厳密にする形へ直しました。

```
GitHub の実 tarball    v0.5.2 = 246 ファイル ／ v0.6.2 = 284 ファイル
HEAD の source          289 ファイル
macOS tar が作ったもの   通る（生バイナリ xattr を含む）
```

壊れた tar の corpus は **26 個 6 種類 → 39 個 8 種類**に増えました。
v8 で `safe` だったものの結末は変わっていません。

---

## 数えたもの

| | v0.6.3 | v0.6.4 |
|---|---:|---:|
| 単体テスト | 770 | **798**（skip 0 / 失敗 0） |
| 壊れた tar の材料 | 26 個 6 種類 | **39 個 8 種類** |
| 検証対象（`validate:profiles`） | 14 | **14** |
| schema | 22 | **22**（BUMP 0） |
| 記録された入力 | 32 | **32** |
| 検算ツール | `toolVersion` 8 | **9** |
| 根拠の区分 | FACT 53 / DERIVED 28 / ASSUMPTION 54 | **変わらず** |
| 区間 / event | TRS 23/32・TRRS 30/36 | **変わらず** |

---

## 変わっていないもの

- **モデルの数値**。接点位置も走査結果も同じです（上の葉ノード比較）。
- **`verifiedPhysical` は `false`**。実測記録は依然 **0 件**で、それが正しい状態です。
- 過去 release の asset。**v0.1.0 以降の 293 件すべて byte 一致**であることを、
  再生成の段ごとに対照つきで実測しています。
- `ARCHIVE_INVALID` は v1 の自己申告 schema では表現できないままです（→ v0.6.1 notes）。
  `status` の種類は増えていません。

---

## まだやっていないこと

- **実測が 1 件も来ていません。**ジャック内部の接点位置は全部仮定のままです。
- **導通測定も音響測定もしていません。**
- 物理検証の evidence asset と、profile への `evidenceSources` 出力。
- annotated tag への署名。
- `source-verification-result` の v2 化。
- **`tar` の oracle が 2 実装しかありません。**GNU tar・BusyBox では確かめていません。
- 入力の閉包検査は `import` グラフと文字列リテラルの静的走査の 2 本立てで、
  動的 `import()`・変数で組み立てたパス・環境変数経由は見えません。
- 検算ツールの強化は **試験した範囲まで**です。**すべての細工に耐えることは示していません**
  ——現に v5 で 3 つ、v6 で 2 つ、v7 で 3 つ、v8 で 3 つ見落としていました。
  **v8 では検査の側（差分試験）にも穴がありました。**
