# `verifiedPhysical` の合格条件（条文 v2）

> この文書の HTML 版（同名 `.html`）は `npm run docs:html` で生成しています。**HTML を直接編集しないでください。**

作成 2026-08-06 ／ **条文 v2（同日改訂）** ／ 実装 `scripts/measurementGate.mjs` ／ 台帳 `docs/measurements/measurement-records.v1.json`

> **v1 は穴が 3 つ空いていました。**外部監査（2026-08-06）が反例を出し、こちらで再現してから直しています。
> **記録が 0 件のうちに直しました。**`true` になった実績はまだ 1 件もありません。
>
> | | v1 で何が起きたか | v2 |
> |---|---|---|
> | 相反する記録 | 一致する記録を 1 件見つけた時点で止めていたので、**矛盾する記録が同じ台帳にあっても `true`**。しかも**並び順で結果が変わった** | 全候補を評価する。一致と矛盾が併存したら `AMBIGUOUS`（＝ `false`）→ **第10条** |
> | 測定の分解能 | 「分解能が書いてある」しか見ていないので、**許容 0.29 mm の判定を分解能 1.0 mm の道具が通った** | 観測点ごとに要求分解能を許容から導き、超えたら認定しない → **第5条** |
> | 主張の範囲 | `verifiedPhysical: true` が「接点トポロジーを実物で確かめた」と読めた | `claimScope` を出力し、**幾何の観測点が合っただけ**であることを機械可読にする → **第9条** |

## なぜ要るのか

配布 profile の `modelLimitations.verifiedPhysical` は、**2026-08-06 まで生成器のリテラル `false`** でした。
schema の説明はこう書いてあるだけです。

> 実物と突き合わせて検証済みか。未実測の profile では必ず false。**true にできるのは実測記録が伴う場合のみ**

**何を何点測れば足りるのかが、どこにも書いてありませんでした。**
つまり実態は「**誰かがリテラルを書き換えたら `true`**」で、それは検証ではありません。

この条文は、その 1 行を**記録から機械で決まる値**に変えるためのものです。

> **急ぐ話ではありません。**実測は募集していますが必須にしていません（`docs/VERIFICATION_PLAN.md` 冒頭）。
> **記録が 0 件なら `false` のままで、それが正しい状態です。**
> 条文を先に書くのは、**記録が来た日に判断が要らないようにする**ためです。
> 判断が要る形にしておくと、たいてい甘い方へ倒れます。

---

## 第1条（何を主張するか）

`verifiedPhysical: true` は、**その profile の必須観測点がすべて実測で裏付けられた**ことだけを言う。

**「この profile のすべての値が実測である」とは言わない。**
現に TRS profile の `evidenceGrade` は `ASSUMPTION` が 93 件、TRRS は 151 件ある（2026-08-05 実測）。
必須観測点が全部埋まっても、この数は 0 にならない。

**「モデルと実物が一致した」を含む。**単に「突き合わせた」ではない。
実測が予測から離れていたら、**モデルのほうを直してから**でなければ `true` にならない（→ 第4条）。

## 第2条（必須観測点）

profile ごとに、必須観測点を名指しで決める。`scripts/measurementGate.mjs` の `REQUIRED_FOR_PROFILE` が正本。

| profile | 必須観測点 |
|---|---|
| `TRS\|JACK-TRS` | `RING_BREAK_OPEN_DEPTH_MM` ／ `TIP_BREAK_OPEN_DEPTH_MM` |
| `TRS\|JACK-TRRS` | `L_FIRST_CONTACT_SHOULDER_GAP_MM` |

**必須観測点が定義されていない profile は、必ず `false` になる。**
「必須が 0 件だから全部満たしている」で `true` にすると、
**profile を 1 本足しただけで検証済みを名乗れてしまう。**空集合は合格にしない。

> **`TRS|JACK-TRRS` の必須観測点は、別の variant（`TRRS-CTIA|JACK-TRRS`）の量です。**
> 4極ジャックの接点位置を決める測定なので、3極プラグを挿した profile の event 列には現れません。
> 生成器がその variant を別に評価して予測を渡します。**渡されなければ満たせません。**

## 第3条（記録が有効であるための条件）

次を**すべて**満たす記録だけを数える。1 つでも欠ければ、その記録は使わない。

| | 条件 | なぜ |
|---|---|---|
| a | `observation` が条文に定義されている | 定義していない量を根拠にしない |
| b | `variantId` が観測点の定義と一致する | 別の組み合わせで測った値を混ぜない |
| c | **生値が 3 回以上**ある（平均だけの記録は受け取らない） | ばらつきが見えないと、その値の信用度が誰にも分からない |
| d | 測定器の**分解能**が書かれている | c のばらつきが許容内かを判断できない |
| e | **ばらつき（3 回の最大−最小）が許容以内** | → 第5条 |
| f | 測った人・測定日・ジャックとプラグの型番がある | 誰の記録かを追えるようにする |
| g | `retracted: true` でない | 取り下げた記録は消さずに残す。消すと、一度 `true` になっていた事実が追えない |

### **「使えない」を 2 つに分ける（v2）**

| | 意味 | どう扱うか |
|---|---|---|
| **記録として壊れている** | 上の a〜f が欠けている | **中身を読まない。**数えない |
| **認定には足りない** | 読めるが、分解能が粗い・生値が目盛に乗っていない（→ 第5条） | **`true` にはしない。**ただし**モデルとの食い違いは見る** |

分けるのは、**粗い測定でもモデルの誤りは検出できる**からである。
分解能 1 mm の道具でも「予測より 1.45 mm ずれている」は言える。
それを「使えない記録」として捨てると、**測ってもらったのにモデルの誤りを見逃す。**
`true` を出す資格と、警報を鳴らす資格は別に置く。

## 第4条（一致の判定）

有効な記録の**平均**と、**そのとき生成器が計算した現行モデルの予測**の差が、
許容以内であることを求める。

```
認定する      |平均(生値) − 現行モデルの予測| ≤ 許容
矛盾と見なす  |平均(生値) − 現行モデルの予測| >  許容 + 分解能/2
```

**予測は artifact から引かない。生成器がその場で計算した値を使う。**
artifact から引くと、モデルを直したのに古い予測と照合し続けることが起きる。

**差が許容を超えたら `false` になる。**そのとき直すのは記録ではなく**モデル**である。
記録のほうを合わせにいったら、この条文は意味を失う。

> **矛盾の判定にだけ `分解能/2` を足すのは（v2）**、目盛の量子化で最大それだけずれるからである。
> 足さないと、**粗い道具で測っただけでモデルが間違っていることになる。**
> 逆に認定側へ足すと、粗い道具ほど通りやすくなる。**片側にだけ足す。**

## 第5条（許容と、要求する分解能）

許容は、**その観測点で見分けたい差の 1/5** を基準に置く。
**要求する分解能は、その許容の 1/3 以下**——ただし実在する道具の刻みへ切り下げる（v2）。

| 観測点 | 見分けたい差 | 許容 | **要求分解能** |
|---|---:|---:|---:|
| `L_FIRST_CONTACT_SHOULDER_GAP_MM` | **1.45 mm**（本モデル 2.14 mm ↔ 実在部品 PS000001 由来 0.69 mm） | **0.29 mm** | **0.05 mm 以下** |
| `RING_BREAK_OPEN_DEPTH_MM` | 約 1.5 mm | 0.3 mm | 0.1 mm 以下 |
| `TIP_BREAK_OPEN_DEPTH_MM` | 約 1.5 mm | 0.3 mm | 0.1 mm 以下 |

**判定に効かない桁を要求しない。**`L` はデジタルノギスの分解能 0.01 mm の **145 倍**の差を見る測定で、
0.1 mm 単位で読んでも 14.5 倍ある。ここで 0.02 mm の一致を求めると、
**測ってもらったのに弾く**ことになる。

**`docs/VERIFICATION_PLAN.md` が求めているデジタルノギス（0.01 mm）は、3 観測点すべてで通る。**
表の値は手で書いていない。許容から計算して、`0.001 / 0.002 / 0.005 / 0.01 / 0.02 / 0.05 / 0.1 / 0.2 / 0.5 / 1` の中へ切り下げている。

### なぜ分解能を要求するのか — v1 で起きたこと

v1 は「分解能が書いてある」しか見ていなかった。**分解能 1.0 mm、生値 2.0 mm ×3** の記録が
許容 0.29 mm の判定を**通った**（外部監査の反例。こちらで再現済み）。
1 mm 刻みで「2」と読んだ値は、真値が 1.5〜2.5 のどこでもありうる。
**許容 0.29 mm の一致を、それで名乗ってはいけない。**

**生値が目盛に乗っていることも見る。**分解能 0.05 mm の道具で「2.143」は読めない。
読めない値が書いてあるなら、分解能か生値のどちらかが違う。

## 第6条（台帳と profile がずれないようにする）

台帳は `inputDigest` に**入れていない**。**理由を書いておく。**

`docs/measurements/measurement-records.v1.json` を共通入力に足すと、
**記録が 1 件も無い今の時点で、全 artifact の ID が動く。**
中身が変わっていないのに ID が変わる状態を自分から作ることになる
（`source-input-scope.v1.json` の `commonInputsNote` が避けている問題と同じ型）。

代わりに、**profile 自身に判定の結果を書き込む**。

```
modelLimitations.verifiedPhysical        判定結果
modelLimitations.physicalVerificationRef 台帳のパスと sha256・条文の版・満たした観測点・欠けている観測点
```

そのうえで `npm run validate:profiles` の semantic 規則 `measurementRecords` が、
**現在の台帳から判定をやり直して、profile に書いてある値と突き合わせる。**
台帳だけ書き換えて profile を作り直さなければ、そこで落ちる。

> **`inputDigest` に入れるほうが素直ではあります。**記録が実際に入って
> `verifiedPhysical` が動くようになったら、そのタイミングで入れ直すのが筋です。
> **いまは入れない理由が「空だから」なので、空でなくなったら理由も消えます。**

## 第9条（この判定が主張する範囲）— **v2 で新設**

判定器は `claimScope` を出す。値は **`geometry-only`** で固定である。

```
verifiedPhysical: true が言うこと
  → 条文が名指しした幾何量が、現行モデルの予測と、条文の許容の内側で合った

言わないこと
  → target-topology  どの端子がどの導体と繋がるか・GND が開放か・短絡が無いか
  → acoustic         音がどうなるか
  → lot-variation    別個体・別ロットでも同じか
```

配布 profile の `physicalVerificationRef` にも `scope=geometry-only` が入る。
**文字列の中に置いてある**のは、`physicalVerificationRef` の型が `["string","null"]` で、
構造を足すと言語が広がって **profile v4（BUMP）**になり下流が止まるためである（→ 第6条・`docs/SCHEMA_VERSIONING_POLICY.md`）。

### なぜ書くのか

`TRS|JACK-TRRS` profile の必須観測点は `L_FIRST_CONTACT_SHOULDER_GAP_MM` **1 点だけ**で、
しかも**別 variant（`TRRS-CTIA|JACK-TRRS`）の量**である（→ 第2条）。

この 1 点が合っても、`IV028`（左右が別導体・共通帰線だけ開放）の次は**何も確かめていない。**

| | 確かめたか |
|---|---|
| L が期待した端子に繋がる | いいえ |
| R が期待した端子に繋がる | いいえ |
| L と R が別の導体である | いいえ |
| GND が開いている | いいえ |
| 信号どうし・信号と帰線が短絡していない | いいえ |

**この profile を使う側が `verifiedPhysical: true` を「トポロジーを実物で確かめた」と読むと、間違える。**
だから機械可読で範囲を出す。トポロジーを確かめたければ、
`docs/VERIFICATION_PLAN.md` §2-2 の導通測定が要る（**まだ 1 件も来ていない**）。

## 第10条（相反する記録の扱い）— **v2 で新設**

**必須観測点ごとに、候補となる記録を全部評価する。**一致を 1 件見つけても止めない。

| 状態 | 判定 |
|---|---|
| 一致する記録があり、矛盾する記録が無い | `VERIFIED` |
| **一致する記録と矛盾する記録が併存する** | **`AMBIGUOUS`（＝ `false`）** |
| 一致する記録が無い | `UNVERIFIED` |
| 台帳に `recordId` の重複がある | **`INVALID_LEDGER`（＝ `false`）** |

**結果は記録の並び順に依存しない。**`recordId` で並べ直してから評価する。

v1 は一致を 1 件見つけた時点で止めていたので、次の 2 つが起きていた（実測）。

```
[一致, 矛盾] → verified=true  rejected=0 件   ← 矛盾する記録が評価すらされない
[矛盾, 一致] → verified=true  rejected=1 件   ← 同じ台帳なのに出力が違う
```

**どちらも `true` である**ことが問題である。矛盾は「弾いて先へ進む」ものではない。

### 併存したときにどうするか

**`supersedes` は入れない。**新しい記録が古い記録を黙って上書きできる仕組みは作らない。
決着のさせ方は 2 つだけで、どちらも**書いた人の判断が残る。**

1. **モデルのほうが間違っている** → モデルを直す。直せば矛盾は消える（第4条）
2. **その記録が間違っていた** → `retracted: true` と `retractedReason` を付ける。**記録は消さない**

`retracted` は既に台帳 schema にある。**新しい欄を足さずに済む**ので、
`measurement-record.v1` の版も上がらない（`additionalProperties: false` なので、欄を足すと BUMP になる）。

## 第7条（この条文の限界）

- **必須観測点の選び方そのものは、この条文では正当化していない。**
  「看板の結論に効く」という判断で選んでいて、**別の選び方もありうる。**
- **1 人が 1 個の部品を 3 回測った記録**で `true` になる。
  個体差・ロット差は見ていない。**複数個体を要求していない。**
- **`true` は幾何量についてしか言わない**（→ 第9条）。接点トポロジーも音響も含まない。
- **矛盾の検出は、台帳に記録が入った分しか働かない。**測って矛盾に気づいた人が
  記録を出さなければ、この条文は何も言わない。
- 予測と一致したことは示せるが、**一致した理由がモデルの正しさとは限らない**
  （たまたま合うパラメータ組み合わせが他にもありうる）。
- **`true` になっても、`evidenceGrade` の値そのものは動かない**（→ 第8条）。

**「起きない」とは書かない。**この条文で言えるのは
**「いま定義した観測点については、記録が無ければ `true` にならない」まで**である。

---

## 第8条（実測値をどの区分へ置くか）— **2026-08-06 決定**

**実測値も `FACT` に置く。区分は増やさない。**

`FACT` の意味を「メーカーのデータシート・図面の**記載寸法**」から
「**一次情報**（記載寸法、および第3条〜第5条どおりに受け取った実測記録）」へ広げる。

### 記載寸法と実測は `sources` で分ける

```
記載寸法  sources: ["ds-1532-10"]   一次資料の ID
実測      sources: ["MR0001"]       docs/measurements/measurement-records.v1.json の記録 ID
```

**`grade` を見て区別しない。`sources` を見る。**下流が両者を分ける必要がある場面は実在する。

| 場面 | なぜ分けたいか |
|---|---|
| 第三者が独立に再現する | 記載寸法は誰でも同じ値を引けるが、**実測は 1 個体・1 回**のもの |
| 公差の扱い | 記載値の公差（φ3.5±0.05）と、実測の**ばらつき**は意味が違う |
| 別部品へ一般化する | 記載値は型番全体の仕様、実測値は**その個体**。混ぜると一般化の根拠が壊れる |

**そのすべてが `sources` で足りる。**区分を増やす必要はない。

### なぜ区分を増やさなかったか — 実測して決めた

条文の判定器（`scripts/schemaLanguageDiff.mjs`）に両案を当てた（2026-08-06 実測）。

| 案 | 変更 | 判定 |
|---|---|---|
| **B（採用）** | `grade` の `description` を広げる | **`HOLD`**（差分 0 件・版据え置き） |
| A（見送り） | `enum` へ `MEASURED` を足す | **`BUMP`**（profile v4・**下流が止まる**） |

対照: `enum` から 1 つ削ると `HOLD_RECORD`、同一 schema 同士は `HOLD`。

**A を見送った理由は「いま入れる値が無い」ことである。**
2026-08-06 時点の `FACT` **53 件はすべて記載寸法で、実測由来は 0 件**（実測）。
中身が空のうちに下流を止めると、**止める理由を実物で説明できない。**

**実測記録が入り、`sources` では足りない場面が実際に出たら、そのとき A を検討する。**
そのときは理由を実物で示せる。
